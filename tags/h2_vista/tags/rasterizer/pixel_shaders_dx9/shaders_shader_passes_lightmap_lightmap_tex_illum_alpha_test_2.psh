; compiled from tags\rasterizer\pixel_shaders_dx9\shaders_shader_passes_lightmap_lightmap_tex_illum_alpha_test_2.txt
;
#const C2.rgb= $lightmap_direct_color
#const C3.rgb= $lightmap_indirect_color
#const C4.rgb= $specular_color
#const C5.rgb= $specular_glancing_color
#const C6.rgb= $lightmap_specular_color_times_one_half
#const C7.rgb= $lightmap_ambient_color
#const C8.rgb= $self_illum_color
#case $specular_type=0
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 c8 : register(C8);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=1 term=1
    // line 27: R0= EXPAND(T2).EXPAND(D0)
    float temp0;
    float3 temp1;
    temp1= 2 * saturate(tex2.rgb) - 1;
    float3 temp2;
    float3 temp3;
    temp3= 2 * saturate(color0.rgb) - 1;
    float3 temp4;
    float temp5;
    temp5= dot(temp1, temp3);
    temp0= temp5;
    // exp.components=1 term=1
    // line 28: R1= EXPAND(T3).EXPAND(D0)
    float temp6;
    float3 temp7;
    temp7= 2 * saturate(tex3.rgb) - 1;
    float3 temp8;
    float3 temp9;
    temp9= 2 * saturate(color0.rgb) - 1;
    float3 temp10;
    float temp11;
    temp11= dot(temp7, temp9);
    temp6= temp11;
    // exp.components=1 term=1
    // line 29: R0a= T4a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex4.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    // exp.components=1 term=1
    // line 30: T3a= C0a
    float temp18;
    float temp19;
    temp19= 0.752941;
    float3 temp20;
    temp18= temp19;
    r0.rgb= temp0;
    r1.rgb= temp6;
    r0.a= temp12;
    tex3.a= temp18;
    // exp.components=1 term=1
    // line 33: T2= EXPAND(T2).EXPAND(T3)
    float temp21;
    float3 temp22;
    temp22= 2 * saturate(tex2.rgb) - 1;
    float3 temp23;
    float3 temp24;
    temp24= 2 * saturate(tex3.rgb) - 1;
    float3 temp25;
    float temp26;
    temp26= dot(temp22, temp24);
    temp21= temp26;
    // exp.components=1 term=1
    // line 34: T3= EXPAND(T2).EXPAND(D1)
    float temp27;
    float3 temp28;
    temp28= 2 * saturate(tex2.rgb) - 1;
    float3 temp29;
    float3 temp30;
    temp30= 2 * saturate(color1.rgb) - 1;
    float3 temp31;
    float temp32;
    temp32= dot(temp28, temp30);
    temp27= temp32;
    // exp.components=1 term=1
    // line 35: D0a= R0b
    float temp33;
    float temp34;
    temp34= saturate(r0.b);
    float3 temp35;
    temp33= temp34;
    // exp.components=1 term=1
    // line 36: T2a= EXPAND(T2b)
    float temp36;
    float temp37;
    temp37= 2 * saturate(tex2.b) - 1;
    float3 temp38;
    temp36= temp37;
    tex2.rgb= temp21;
    tex3.rgb= temp27;
    color0.a= temp33;
    tex2.a= temp36;
    // exp.components=3 term=3 term=3
    // line 41: R0= T2*C0 + T3*C1
    float3 temp39;
    float3 temp40;
    float3 temp41;
    float3 temp42;
    temp42= saturate(tex2.rgb);
    float3 temp43;
    float3 temp44;
    temp44= c2.rgb;
    float3 temp45;
    float3 temp46;
    temp46= temp42 * temp44;
    temp40= temp46;
    float3 temp47;
    temp47= saturate(tex3.rgb);
    float3 temp48;
    float3 temp49;
    temp49= c3.rgb;
    float3 temp50;
    float3 temp51;
    temp51= temp47 * temp49;
    temp41= temp51;
    temp39= temp40 + temp41;
    // exp.components=1 term=1 term=1
    // line 42: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp52;
    float temp53;
    float temp54;
    float temp55;
    temp55= tex2.b;
    float3 temp56;
    float temp57;
    temp57= r0.b;
    float3 temp58;
    float temp59;
    temp59= temp55 * temp57;
    temp53= temp59;
    float temp60;
    temp60= 0.5;
    float3 temp61;
    float temp62;
    temp62= r1.b;
    float3 temp63;
    float temp64;
    temp64= temp60 * temp62;
    temp54= temp64;
    temp52= temp53 - temp54;
    temp52= 2 * temp52;
    r0.rgb= temp39;
    r1.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: D1= D0a*C0 + INVERT(D0a)*C1
    float3 temp65;
    float3 temp66;
    float3 temp67;
    float temp68;
    temp68= saturate(color0.a);
    float3 temp69;
    float3 temp70;
    temp70= c4.rgb;
    float3 temp71;
    float3 temp72;
    temp72= temp68 * temp70;
    temp66= temp72;
    float temp73;
    temp73= 1-saturate(color0.a);
    float3 temp74;
    float3 temp75;
    temp75= c5.rgb;
    float3 temp76;
    float3 temp77;
    temp77= temp73 * temp75;
    temp67= temp77;
    temp65= temp66 + temp67;
    // exp.components=1 term=1
    // line 48: D0a= R1a*R1a
    float temp78;
    float temp79;
    temp79= saturate(r1.a);
    float3 temp80;
    float temp81;
    temp81= saturate(r1.a);
    float3 temp82;
    float temp83;
    temp83= temp79 * temp81;
    temp78= temp83;
    // exp.components=1 term=1
    // line 49: T1a= EXPAND_NEGATE(T2b)
    float temp84;
    float temp85;
    temp85= 1 - 2 * saturate(tex2.b);
    float3 temp86;
    temp84= temp85;
    color1.rgb= temp65;
    color0.a= temp78;
    tex1.a= temp84;
    // exp.components=3 term=3
    // line 53: D1= OUT_SCALE_BY_2(D1*C0)
    float3 temp87;
    float3 temp88;
    temp88= saturate(color1.rgb);
    float3 temp89;
    float3 temp90;
    temp90= c6.rgb;
    float3 temp91;
    float3 temp92;
    temp92= temp88 * temp90;
    temp87= temp92;
    temp87= 2 * temp87;
    // exp.components=1 term=1
    // line 54: R1a= D0a*D0a
    float temp93;
    float temp94;
    temp94= saturate(color0.a);
    float3 temp95;
    float temp96;
    temp96= saturate(color0.a);
    float3 temp97;
    float temp98;
    temp98= temp94 * temp96;
    temp93= temp98;
    // exp.components=1 term=1
    // line 55: T1a= EXPAND(T1a)
    float temp99;
    float temp100;
    temp100= 2 * saturate(tex1.a) - 1;
    float3 temp101;
    temp99= temp100;
    color1.rgb= temp87;
    r1.a= temp93;
    tex1.a= temp99;
    // exp.components=3 term=3
    // line 62: T2= T2a*C0
    float3 temp102;
    float temp103;
    temp103= saturate(tex2.a);
    float3 temp104;
    float3 temp105;
    temp105= c7.rgb;
    float3 temp106;
    float3 temp107;
    temp107= temp103 * temp105;
    temp102= temp107;
    // exp.components=3 term=3
    // line 63: D1= T1*C1
    float3 temp108;
    float3 temp109;
    temp109= saturate(tex1.rgb);
    float3 temp110;
    float3 temp111;
    temp111= c8.rgb;
    float3 temp112;
    float3 temp113;
    temp113= temp109 * temp111;
    temp108= temp113;
    // exp.components=1 term=1
    // line 65: R1a= 0
    float temp114;
    float temp115;
    temp115= 0;
    float3 temp116;
    temp114= temp115;
    tex2.rgb= temp102;
    color1.rgb= temp108;
    r1.a= temp114;
    // exp.components=3 term=3 term=3
    // line 93: R0= OUT_SCALE_BY_2(R0*T0 + T2*T0)
    float3 temp117;
    float3 temp118;
    float3 temp119;
    float3 temp120;
    temp120= saturate(r0.rgb);
    float3 temp121;
    float3 temp122;
    temp122= saturate(tex0.rgb);
    float3 temp123;
    float3 temp124;
    temp124= temp120 * temp122;
    temp118= temp124;
    float3 temp125;
    temp125= saturate(tex2.rgb);
    float3 temp126;
    float3 temp127;
    temp127= saturate(tex0.rgb);
    float3 temp128;
    float3 temp129;
    temp129= temp125 * temp127;
    temp119= temp129;
    temp117= temp118 + temp119;
    temp117= 2 * temp117;
    // exp.components=1 term=1
    // line 94: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp130;
    float temp131;
    temp131= saturate(r1.a);
    float3 temp132;
    float temp133;
    temp133= saturate(tex0.a);
    float3 temp134;
    float temp135;
    temp135= temp131 * temp133;
    temp130= temp135;
    temp130= 2 * temp130;
    // exp.components=1 term=1
    // line 95: T1a= EXPAND(T1a)
    float temp136;
    float temp137;
    temp137= 2 * saturate(tex1.a) - 1;
    float3 temp138;
    temp136= temp137;
    r0.rgb= temp117;
    r1.a= temp130;
    tex1.a= temp136;
    // exp.components=3 term=3
    // line 98: D0= OUT_SCALE_BY_2(D1*R1a)
    float3 temp139;
    float3 temp140;
    temp140= saturate(color1.rgb);
    float3 temp141;
    float temp142;
    temp142= saturate(r1.a);
    float3 temp143;
    float3 temp144;
    temp144= temp140 * temp142;
    temp139= temp144;
    temp139= 2 * temp139;
    // exp.components=1 term=1 term=1
    // line 100: T2a= 0 mux 1
    float temp145;
    float temp146;
    float temp147;
    float temp148;
    temp148= 0;
    float3 temp149;
    temp146= temp148;
    float temp150;
    temp150= 1;
    float3 temp151;
    temp147= temp150;
    temp145= lerp(temp146,temp147,round(r0.a));
    color0.rgb= temp139;
    tex2.a= temp145;
    // exp.components=3 term=3 term=3
    // line 102: SRCCOLOR= D0*INVERT(T1a) + [R0+D1]
    float3 temp152;
    float3 temp153;
    float3 temp154;
    float3 temp155;
    temp155= saturate(color0.rgb);
    float3 temp156;
    float temp157;
    temp157= 1-saturate(tex1.a);
    float3 temp158;
    float3 temp159;
    temp159= temp155 * temp157;
    temp153= temp159;
    float3 temp160;
    float3 temp161;
    temp161= r0.rgb+color1.rgb;
    temp160= temp161;
    temp154= temp160;
    temp152= temp153 + temp154;
    // exp.components=1 term=1
    // line 103: SRCALPHA= T2a
    float temp162;
    float temp163;
    temp163= saturate(tex2.a);
    float3 temp164;
    temp162= temp163;
    r0.rgb= temp152;
    r0.a= temp162;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=1
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 c8 : register(C8);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=1 term=1
    // line 27: R0= EXPAND(T2).EXPAND(D0)
    float temp0;
    float3 temp1;
    temp1= 2 * saturate(tex2.rgb) - 1;
    float3 temp2;
    float3 temp3;
    temp3= 2 * saturate(color0.rgb) - 1;
    float3 temp4;
    float temp5;
    temp5= dot(temp1, temp3);
    temp0= temp5;
    // exp.components=1 term=1
    // line 28: R1= EXPAND(T3).EXPAND(D0)
    float temp6;
    float3 temp7;
    temp7= 2 * saturate(tex3.rgb) - 1;
    float3 temp8;
    float3 temp9;
    temp9= 2 * saturate(color0.rgb) - 1;
    float3 temp10;
    float temp11;
    temp11= dot(temp7, temp9);
    temp6= temp11;
    // exp.components=1 term=1
    // line 29: R0a= T4a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex4.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    // exp.components=1 term=1
    // line 30: T3a= C0a
    float temp18;
    float temp19;
    temp19= 0.752941;
    float3 temp20;
    temp18= temp19;
    r0.rgb= temp0;
    r1.rgb= temp6;
    r0.a= temp12;
    tex3.a= temp18;
    // exp.components=1 term=1
    // line 33: T2= EXPAND(T2).EXPAND(T3)
    float temp21;
    float3 temp22;
    temp22= 2 * saturate(tex2.rgb) - 1;
    float3 temp23;
    float3 temp24;
    temp24= 2 * saturate(tex3.rgb) - 1;
    float3 temp25;
    float temp26;
    temp26= dot(temp22, temp24);
    temp21= temp26;
    // exp.components=1 term=1
    // line 34: T3= EXPAND(T2).EXPAND(D1)
    float temp27;
    float3 temp28;
    temp28= 2 * saturate(tex2.rgb) - 1;
    float3 temp29;
    float3 temp30;
    temp30= 2 * saturate(color1.rgb) - 1;
    float3 temp31;
    float temp32;
    temp32= dot(temp28, temp30);
    temp27= temp32;
    // exp.components=1 term=1
    // line 35: D0a= R0b
    float temp33;
    float temp34;
    temp34= saturate(r0.b);
    float3 temp35;
    temp33= temp34;
    // exp.components=1 term=1
    // line 36: T2a= EXPAND(T2b)
    float temp36;
    float temp37;
    temp37= 2 * saturate(tex2.b) - 1;
    float3 temp38;
    temp36= temp37;
    tex2.rgb= temp21;
    tex3.rgb= temp27;
    color0.a= temp33;
    tex2.a= temp36;
    // exp.components=3 term=3 term=3
    // line 41: R0= T2*C0 + T3*C1
    float3 temp39;
    float3 temp40;
    float3 temp41;
    float3 temp42;
    temp42= saturate(tex2.rgb);
    float3 temp43;
    float3 temp44;
    temp44= c2.rgb;
    float3 temp45;
    float3 temp46;
    temp46= temp42 * temp44;
    temp40= temp46;
    float3 temp47;
    temp47= saturate(tex3.rgb);
    float3 temp48;
    float3 temp49;
    temp49= c3.rgb;
    float3 temp50;
    float3 temp51;
    temp51= temp47 * temp49;
    temp41= temp51;
    temp39= temp40 + temp41;
    // exp.components=1 term=1 term=1
    // line 42: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp52;
    float temp53;
    float temp54;
    float temp55;
    temp55= tex2.b;
    float3 temp56;
    float temp57;
    temp57= r0.b;
    float3 temp58;
    float temp59;
    temp59= temp55 * temp57;
    temp53= temp59;
    float temp60;
    temp60= 0.5;
    float3 temp61;
    float temp62;
    temp62= r1.b;
    float3 temp63;
    float temp64;
    temp64= temp60 * temp62;
    temp54= temp64;
    temp52= temp53 - temp54;
    temp52= 2 * temp52;
    r0.rgb= temp39;
    r1.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: D1= D0a*C0 + INVERT(D0a)*C1
    float3 temp65;
    float3 temp66;
    float3 temp67;
    float temp68;
    temp68= saturate(color0.a);
    float3 temp69;
    float3 temp70;
    temp70= c4.rgb;
    float3 temp71;
    float3 temp72;
    temp72= temp68 * temp70;
    temp66= temp72;
    float temp73;
    temp73= 1-saturate(color0.a);
    float3 temp74;
    float3 temp75;
    temp75= c5.rgb;
    float3 temp76;
    float3 temp77;
    temp77= temp73 * temp75;
    temp67= temp77;
    temp65= temp66 + temp67;
    // exp.components=1 term=1
    // line 48: D0a= R1a*R1a
    float temp78;
    float temp79;
    temp79= saturate(r1.a);
    float3 temp80;
    float temp81;
    temp81= saturate(r1.a);
    float3 temp82;
    float temp83;
    temp83= temp79 * temp81;
    temp78= temp83;
    // exp.components=1 term=1
    // line 49: T1a= EXPAND_NEGATE(T2b)
    float temp84;
    float temp85;
    temp85= 1 - 2 * saturate(tex2.b);
    float3 temp86;
    temp84= temp85;
    color1.rgb= temp65;
    color0.a= temp78;
    tex1.a= temp84;
    // exp.components=3 term=3
    // line 53: D1= OUT_SCALE_BY_2(D1*C0)
    float3 temp87;
    float3 temp88;
    temp88= saturate(color1.rgb);
    float3 temp89;
    float3 temp90;
    temp90= c6.rgb;
    float3 temp91;
    float3 temp92;
    temp92= temp88 * temp90;
    temp87= temp92;
    temp87= 2 * temp87;
    // exp.components=1 term=1
    // line 54: R1a= D0a*D0a
    float temp93;
    float temp94;
    temp94= saturate(color0.a);
    float3 temp95;
    float temp96;
    temp96= saturate(color0.a);
    float3 temp97;
    float temp98;
    temp98= temp94 * temp96;
    temp93= temp98;
    // exp.components=1 term=1
    // line 55: T1a= EXPAND(T1a)
    float temp99;
    float temp100;
    temp100= 2 * saturate(tex1.a) - 1;
    float3 temp101;
    temp99= temp100;
    color1.rgb= temp87;
    r1.a= temp93;
    tex1.a= temp99;
    // exp.components=3 term=3
    // line 70: T2= T2a*C0
    float3 temp102;
    float temp103;
    temp103= saturate(tex2.a);
    float3 temp104;
    float3 temp105;
    temp105= c7.rgb;
    float3 temp106;
    float3 temp107;
    temp107= temp103 * temp105;
    temp102= temp107;
    // exp.components=3 term=3
    // line 71: D1= T1*C1
    float3 temp108;
    float3 temp109;
    temp109= saturate(tex1.rgb);
    float3 temp110;
    float3 temp111;
    temp111= c8.rgb;
    float3 temp112;
    float3 temp113;
    temp113= temp109 * temp111;
    temp108= temp113;
    // exp.components=1 term=1
    // line 73: R1a= R1a*R1a
    float temp114;
    float temp115;
    temp115= saturate(r1.a);
    float3 temp116;
    float temp117;
    temp117= saturate(r1.a);
    float3 temp118;
    float temp119;
    temp119= temp115 * temp117;
    temp114= temp119;
    tex2.rgb= temp102;
    color1.rgb= temp108;
    r1.a= temp114;
    // exp.components=3 term=3 term=3
    // line 93: R0= OUT_SCALE_BY_2(R0*T0 + T2*T0)
    float3 temp120;
    float3 temp121;
    float3 temp122;
    float3 temp123;
    temp123= saturate(r0.rgb);
    float3 temp124;
    float3 temp125;
    temp125= saturate(tex0.rgb);
    float3 temp126;
    float3 temp127;
    temp127= temp123 * temp125;
    temp121= temp127;
    float3 temp128;
    temp128= saturate(tex2.rgb);
    float3 temp129;
    float3 temp130;
    temp130= saturate(tex0.rgb);
    float3 temp131;
    float3 temp132;
    temp132= temp128 * temp130;
    temp122= temp132;
    temp120= temp121 + temp122;
    temp120= 2 * temp120;
    // exp.components=1 term=1
    // line 94: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp133;
    float temp134;
    temp134= saturate(r1.a);
    float3 temp135;
    float temp136;
    temp136= saturate(tex0.a);
    float3 temp137;
    float temp138;
    temp138= temp134 * temp136;
    temp133= temp138;
    temp133= 2 * temp133;
    // exp.components=1 term=1
    // line 95: T1a= EXPAND(T1a)
    float temp139;
    float temp140;
    temp140= 2 * saturate(tex1.a) - 1;
    float3 temp141;
    temp139= temp140;
    r0.rgb= temp120;
    r1.a= temp133;
    tex1.a= temp139;
    // exp.components=3 term=3
    // line 98: D0= OUT_SCALE_BY_2(D1*R1a)
    float3 temp142;
    float3 temp143;
    temp143= saturate(color1.rgb);
    float3 temp144;
    float temp145;
    temp145= saturate(r1.a);
    float3 temp146;
    float3 temp147;
    temp147= temp143 * temp145;
    temp142= temp147;
    temp142= 2 * temp142;
    // exp.components=1 term=1 term=1
    // line 100: T2a= 0 mux 1
    float temp148;
    float temp149;
    float temp150;
    float temp151;
    temp151= 0;
    float3 temp152;
    temp149= temp151;
    float temp153;
    temp153= 1;
    float3 temp154;
    temp150= temp153;
    temp148= lerp(temp149,temp150,round(r0.a));
    color0.rgb= temp142;
    tex2.a= temp148;
    // exp.components=3 term=3 term=3
    // line 102: SRCCOLOR= D0*INVERT(T1a) + [R0+D1]
    float3 temp155;
    float3 temp156;
    float3 temp157;
    float3 temp158;
    temp158= saturate(color0.rgb);
    float3 temp159;
    float temp160;
    temp160= 1-saturate(tex1.a);
    float3 temp161;
    float3 temp162;
    temp162= temp158 * temp160;
    temp156= temp162;
    float3 temp163;
    float3 temp164;
    temp164= r0.rgb+color1.rgb;
    temp163= temp164;
    temp157= temp163;
    temp155= temp156 + temp157;
    // exp.components=1 term=1
    // line 103: SRCALPHA= T2a
    float temp165;
    float temp166;
    temp166= saturate(tex2.a);
    float3 temp167;
    temp165= temp166;
    r0.rgb= temp155;
    r0.a= temp165;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=2
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 c8 : register(C8);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=1 term=1
    // line 27: R0= EXPAND(T2).EXPAND(D0)
    float temp0;
    float3 temp1;
    temp1= 2 * saturate(tex2.rgb) - 1;
    float3 temp2;
    float3 temp3;
    temp3= 2 * saturate(color0.rgb) - 1;
    float3 temp4;
    float temp5;
    temp5= dot(temp1, temp3);
    temp0= temp5;
    // exp.components=1 term=1
    // line 28: R1= EXPAND(T3).EXPAND(D0)
    float temp6;
    float3 temp7;
    temp7= 2 * saturate(tex3.rgb) - 1;
    float3 temp8;
    float3 temp9;
    temp9= 2 * saturate(color0.rgb) - 1;
    float3 temp10;
    float temp11;
    temp11= dot(temp7, temp9);
    temp6= temp11;
    // exp.components=1 term=1
    // line 29: R0a= T4a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex4.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    // exp.components=1 term=1
    // line 30: T3a= C0a
    float temp18;
    float temp19;
    temp19= 0.752941;
    float3 temp20;
    temp18= temp19;
    r0.rgb= temp0;
    r1.rgb= temp6;
    r0.a= temp12;
    tex3.a= temp18;
    // exp.components=1 term=1
    // line 33: T2= EXPAND(T2).EXPAND(T3)
    float temp21;
    float3 temp22;
    temp22= 2 * saturate(tex2.rgb) - 1;
    float3 temp23;
    float3 temp24;
    temp24= 2 * saturate(tex3.rgb) - 1;
    float3 temp25;
    float temp26;
    temp26= dot(temp22, temp24);
    temp21= temp26;
    // exp.components=1 term=1
    // line 34: T3= EXPAND(T2).EXPAND(D1)
    float temp27;
    float3 temp28;
    temp28= 2 * saturate(tex2.rgb) - 1;
    float3 temp29;
    float3 temp30;
    temp30= 2 * saturate(color1.rgb) - 1;
    float3 temp31;
    float temp32;
    temp32= dot(temp28, temp30);
    temp27= temp32;
    // exp.components=1 term=1
    // line 35: D0a= R0b
    float temp33;
    float temp34;
    temp34= saturate(r0.b);
    float3 temp35;
    temp33= temp34;
    // exp.components=1 term=1
    // line 36: T2a= EXPAND(T2b)
    float temp36;
    float temp37;
    temp37= 2 * saturate(tex2.b) - 1;
    float3 temp38;
    temp36= temp37;
    tex2.rgb= temp21;
    tex3.rgb= temp27;
    color0.a= temp33;
    tex2.a= temp36;
    // exp.components=3 term=3 term=3
    // line 41: R0= T2*C0 + T3*C1
    float3 temp39;
    float3 temp40;
    float3 temp41;
    float3 temp42;
    temp42= saturate(tex2.rgb);
    float3 temp43;
    float3 temp44;
    temp44= c2.rgb;
    float3 temp45;
    float3 temp46;
    temp46= temp42 * temp44;
    temp40= temp46;
    float3 temp47;
    temp47= saturate(tex3.rgb);
    float3 temp48;
    float3 temp49;
    temp49= c3.rgb;
    float3 temp50;
    float3 temp51;
    temp51= temp47 * temp49;
    temp41= temp51;
    temp39= temp40 + temp41;
    // exp.components=1 term=1 term=1
    // line 42: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp52;
    float temp53;
    float temp54;
    float temp55;
    temp55= tex2.b;
    float3 temp56;
    float temp57;
    temp57= r0.b;
    float3 temp58;
    float temp59;
    temp59= temp55 * temp57;
    temp53= temp59;
    float temp60;
    temp60= 0.5;
    float3 temp61;
    float temp62;
    temp62= r1.b;
    float3 temp63;
    float temp64;
    temp64= temp60 * temp62;
    temp54= temp64;
    temp52= temp53 - temp54;
    temp52= 2 * temp52;
    r0.rgb= temp39;
    r1.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: D1= D0a*C0 + INVERT(D0a)*C1
    float3 temp65;
    float3 temp66;
    float3 temp67;
    float temp68;
    temp68= saturate(color0.a);
    float3 temp69;
    float3 temp70;
    temp70= c4.rgb;
    float3 temp71;
    float3 temp72;
    temp72= temp68 * temp70;
    temp66= temp72;
    float temp73;
    temp73= 1-saturate(color0.a);
    float3 temp74;
    float3 temp75;
    temp75= c5.rgb;
    float3 temp76;
    float3 temp77;
    temp77= temp73 * temp75;
    temp67= temp77;
    temp65= temp66 + temp67;
    // exp.components=1 term=1
    // line 48: D0a= R1a*R1a
    float temp78;
    float temp79;
    temp79= saturate(r1.a);
    float3 temp80;
    float temp81;
    temp81= saturate(r1.a);
    float3 temp82;
    float temp83;
    temp83= temp79 * temp81;
    temp78= temp83;
    // exp.components=1 term=1
    // line 49: T1a= EXPAND_NEGATE(T2b)
    float temp84;
    float temp85;
    temp85= 1 - 2 * saturate(tex2.b);
    float3 temp86;
    temp84= temp85;
    color1.rgb= temp65;
    color0.a= temp78;
    tex1.a= temp84;
    // exp.components=3 term=3
    // line 53: D1= OUT_SCALE_BY_2(D1*C0)
    float3 temp87;
    float3 temp88;
    temp88= saturate(color1.rgb);
    float3 temp89;
    float3 temp90;
    temp90= c6.rgb;
    float3 temp91;
    float3 temp92;
    temp92= temp88 * temp90;
    temp87= temp92;
    temp87= 2 * temp87;
    // exp.components=1 term=1
    // line 54: R1a= D0a*D0a
    float temp93;
    float temp94;
    temp94= saturate(color0.a);
    float3 temp95;
    float temp96;
    temp96= saturate(color0.a);
    float3 temp97;
    float temp98;
    temp98= temp94 * temp96;
    temp93= temp98;
    // exp.components=1 term=1
    // line 55: T1a= EXPAND(T1a)
    float temp99;
    float temp100;
    temp100= 2 * saturate(tex1.a) - 1;
    float3 temp101;
    temp99= temp100;
    color1.rgb= temp87;
    r1.a= temp93;
    tex1.a= temp99;
    // exp.components=3 term=3
    // line 78: T2= T2a*C0
    float3 temp102;
    float temp103;
    temp103= saturate(tex2.a);
    float3 temp104;
    float3 temp105;
    temp105= c7.rgb;
    float3 temp106;
    float3 temp107;
    temp107= temp103 * temp105;
    temp102= temp107;
    // exp.components=3 term=3
    // line 79: D1= T1*C1
    float3 temp108;
    float3 temp109;
    temp109= saturate(tex1.rgb);
    float3 temp110;
    float3 temp111;
    temp111= c8.rgb;
    float3 temp112;
    float3 temp113;
    temp113= temp109 * temp111;
    temp108= temp113;
    // exp.components=1 term=1
    // line 81: R1a= D0a
    float temp114;
    float temp115;
    temp115= saturate(color0.a);
    float3 temp116;
    temp114= temp115;
    tex2.rgb= temp102;
    color1.rgb= temp108;
    r1.a= temp114;
    // exp.components=3 term=3 term=3
    // line 93: R0= OUT_SCALE_BY_2(R0*T0 + T2*T0)
    float3 temp117;
    float3 temp118;
    float3 temp119;
    float3 temp120;
    temp120= saturate(r0.rgb);
    float3 temp121;
    float3 temp122;
    temp122= saturate(tex0.rgb);
    float3 temp123;
    float3 temp124;
    temp124= temp120 * temp122;
    temp118= temp124;
    float3 temp125;
    temp125= saturate(tex2.rgb);
    float3 temp126;
    float3 temp127;
    temp127= saturate(tex0.rgb);
    float3 temp128;
    float3 temp129;
    temp129= temp125 * temp127;
    temp119= temp129;
    temp117= temp118 + temp119;
    temp117= 2 * temp117;
    // exp.components=1 term=1
    // line 94: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp130;
    float temp131;
    temp131= saturate(r1.a);
    float3 temp132;
    float temp133;
    temp133= saturate(tex0.a);
    float3 temp134;
    float temp135;
    temp135= temp131 * temp133;
    temp130= temp135;
    temp130= 2 * temp130;
    // exp.components=1 term=1
    // line 95: T1a= EXPAND(T1a)
    float temp136;
    float temp137;
    temp137= 2 * saturate(tex1.a) - 1;
    float3 temp138;
    temp136= temp137;
    r0.rgb= temp117;
    r1.a= temp130;
    tex1.a= temp136;
    // exp.components=3 term=3
    // line 98: D0= OUT_SCALE_BY_2(D1*R1a)
    float3 temp139;
    float3 temp140;
    temp140= saturate(color1.rgb);
    float3 temp141;
    float temp142;
    temp142= saturate(r1.a);
    float3 temp143;
    float3 temp144;
    temp144= temp140 * temp142;
    temp139= temp144;
    temp139= 2 * temp139;
    // exp.components=1 term=1 term=1
    // line 100: T2a= 0 mux 1
    float temp145;
    float temp146;
    float temp147;
    float temp148;
    temp148= 0;
    float3 temp149;
    temp146= temp148;
    float temp150;
    temp150= 1;
    float3 temp151;
    temp147= temp150;
    temp145= lerp(temp146,temp147,round(r0.a));
    color0.rgb= temp139;
    tex2.a= temp145;
    // exp.components=3 term=3 term=3
    // line 102: SRCCOLOR= D0*INVERT(T1a) + [R0+D1]
    float3 temp152;
    float3 temp153;
    float3 temp154;
    float3 temp155;
    temp155= saturate(color0.rgb);
    float3 temp156;
    float temp157;
    temp157= 1-saturate(tex1.a);
    float3 temp158;
    float3 temp159;
    temp159= temp155 * temp157;
    temp153= temp159;
    float3 temp160;
    float3 temp161;
    temp161= r0.rgb+color1.rgb;
    temp160= temp161;
    temp154= temp160;
    temp152= temp153 + temp154;
    // exp.components=1 term=1
    // line 103: SRCALPHA= T2a
    float temp162;
    float temp163;
    temp163= saturate(tex2.a);
    float3 temp164;
    temp162= temp163;
    r0.rgb= temp152;
    r0.a= temp162;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=3
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 c8 : register(C8);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=1 term=1
    // line 27: R0= EXPAND(T2).EXPAND(D0)
    float temp0;
    float3 temp1;
    temp1= 2 * saturate(tex2.rgb) - 1;
    float3 temp2;
    float3 temp3;
    temp3= 2 * saturate(color0.rgb) - 1;
    float3 temp4;
    float temp5;
    temp5= dot(temp1, temp3);
    temp0= temp5;
    // exp.components=1 term=1
    // line 28: R1= EXPAND(T3).EXPAND(D0)
    float temp6;
    float3 temp7;
    temp7= 2 * saturate(tex3.rgb) - 1;
    float3 temp8;
    float3 temp9;
    temp9= 2 * saturate(color0.rgb) - 1;
    float3 temp10;
    float temp11;
    temp11= dot(temp7, temp9);
    temp6= temp11;
    // exp.components=1 term=1
    // line 29: R0a= T4a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex4.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    // exp.components=1 term=1
    // line 30: T3a= C0a
    float temp18;
    float temp19;
    temp19= 0.752941;
    float3 temp20;
    temp18= temp19;
    r0.rgb= temp0;
    r1.rgb= temp6;
    r0.a= temp12;
    tex3.a= temp18;
    // exp.components=1 term=1
    // line 33: T2= EXPAND(T2).EXPAND(T3)
    float temp21;
    float3 temp22;
    temp22= 2 * saturate(tex2.rgb) - 1;
    float3 temp23;
    float3 temp24;
    temp24= 2 * saturate(tex3.rgb) - 1;
    float3 temp25;
    float temp26;
    temp26= dot(temp22, temp24);
    temp21= temp26;
    // exp.components=1 term=1
    // line 34: T3= EXPAND(T2).EXPAND(D1)
    float temp27;
    float3 temp28;
    temp28= 2 * saturate(tex2.rgb) - 1;
    float3 temp29;
    float3 temp30;
    temp30= 2 * saturate(color1.rgb) - 1;
    float3 temp31;
    float temp32;
    temp32= dot(temp28, temp30);
    temp27= temp32;
    // exp.components=1 term=1
    // line 35: D0a= R0b
    float temp33;
    float temp34;
    temp34= saturate(r0.b);
    float3 temp35;
    temp33= temp34;
    // exp.components=1 term=1
    // line 36: T2a= EXPAND(T2b)
    float temp36;
    float temp37;
    temp37= 2 * saturate(tex2.b) - 1;
    float3 temp38;
    temp36= temp37;
    tex2.rgb= temp21;
    tex3.rgb= temp27;
    color0.a= temp33;
    tex2.a= temp36;
    // exp.components=3 term=3 term=3
    // line 41: R0= T2*C0 + T3*C1
    float3 temp39;
    float3 temp40;
    float3 temp41;
    float3 temp42;
    temp42= saturate(tex2.rgb);
    float3 temp43;
    float3 temp44;
    temp44= c2.rgb;
    float3 temp45;
    float3 temp46;
    temp46= temp42 * temp44;
    temp40= temp46;
    float3 temp47;
    temp47= saturate(tex3.rgb);
    float3 temp48;
    float3 temp49;
    temp49= c3.rgb;
    float3 temp50;
    float3 temp51;
    temp51= temp47 * temp49;
    temp41= temp51;
    temp39= temp40 + temp41;
    // exp.components=1 term=1 term=1
    // line 42: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp52;
    float temp53;
    float temp54;
    float temp55;
    temp55= tex2.b;
    float3 temp56;
    float temp57;
    temp57= r0.b;
    float3 temp58;
    float temp59;
    temp59= temp55 * temp57;
    temp53= temp59;
    float temp60;
    temp60= 0.5;
    float3 temp61;
    float temp62;
    temp62= r1.b;
    float3 temp63;
    float temp64;
    temp64= temp60 * temp62;
    temp54= temp64;
    temp52= temp53 - temp54;
    temp52= 2 * temp52;
    r0.rgb= temp39;
    r1.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: D1= D0a*C0 + INVERT(D0a)*C1
    float3 temp65;
    float3 temp66;
    float3 temp67;
    float temp68;
    temp68= saturate(color0.a);
    float3 temp69;
    float3 temp70;
    temp70= c4.rgb;
    float3 temp71;
    float3 temp72;
    temp72= temp68 * temp70;
    temp66= temp72;
    float temp73;
    temp73= 1-saturate(color0.a);
    float3 temp74;
    float3 temp75;
    temp75= c5.rgb;
    float3 temp76;
    float3 temp77;
    temp77= temp73 * temp75;
    temp67= temp77;
    temp65= temp66 + temp67;
    // exp.components=1 term=1
    // line 48: D0a= R1a*R1a
    float temp78;
    float temp79;
    temp79= saturate(r1.a);
    float3 temp80;
    float temp81;
    temp81= saturate(r1.a);
    float3 temp82;
    float temp83;
    temp83= temp79 * temp81;
    temp78= temp83;
    // exp.components=1 term=1
    // line 49: T1a= EXPAND_NEGATE(T2b)
    float temp84;
    float temp85;
    temp85= 1 - 2 * saturate(tex2.b);
    float3 temp86;
    temp84= temp85;
    color1.rgb= temp65;
    color0.a= temp78;
    tex1.a= temp84;
    // exp.components=3 term=3
    // line 53: D1= OUT_SCALE_BY_2(D1*C0)
    float3 temp87;
    float3 temp88;
    temp88= saturate(color1.rgb);
    float3 temp89;
    float3 temp90;
    temp90= c6.rgb;
    float3 temp91;
    float3 temp92;
    temp92= temp88 * temp90;
    temp87= temp92;
    temp87= 2 * temp87;
    // exp.components=1 term=1
    // line 54: R1a= D0a*D0a
    float temp93;
    float temp94;
    temp94= saturate(color0.a);
    float3 temp95;
    float temp96;
    temp96= saturate(color0.a);
    float3 temp97;
    float temp98;
    temp98= temp94 * temp96;
    temp93= temp98;
    // exp.components=1 term=1
    // line 55: T1a= EXPAND(T1a)
    float temp99;
    float temp100;
    temp100= 2 * saturate(tex1.a) - 1;
    float3 temp101;
    temp99= temp100;
    color1.rgb= temp87;
    r1.a= temp93;
    tex1.a= temp99;
    // exp.components=3 term=3
    // line 86: T2= T2a*C0
    float3 temp102;
    float temp103;
    temp103= saturate(tex2.a);
    float3 temp104;
    float3 temp105;
    temp105= c7.rgb;
    float3 temp106;
    float3 temp107;
    temp107= temp103 * temp105;
    temp102= temp107;
    // exp.components=3 term=3
    // line 87: D1= T1*C1
    float3 temp108;
    float3 temp109;
    temp109= saturate(tex1.rgb);
    float3 temp110;
    float3 temp111;
    temp111= c8.rgb;
    float3 temp112;
    float3 temp113;
    temp113= temp109 * temp111;
    temp108= temp113;
    // exp.components=1 term=1 term=1
    // line 89: R1a= OUT_SCALE_BY_4(R1a*R1a - T3a)
    float temp114;
    float temp115;
    float temp116;
    float temp117;
    temp117= saturate(r1.a);
    float3 temp118;
    float temp119;
    temp119= saturate(r1.a);
    float3 temp120;
    float temp121;
    temp121= temp117 * temp119;
    temp115= temp121;
    float temp122;
    temp122= saturate(tex3.a);
    float3 temp123;
    temp116= temp122;
    temp114= temp115 - temp116;
    temp114= 4 * temp114;
    tex2.rgb= temp102;
    color1.rgb= temp108;
    r1.a= temp114;
    // exp.components=3 term=3 term=3
    // line 93: R0= OUT_SCALE_BY_2(R0*T0 + T2*T0)
    float3 temp124;
    float3 temp125;
    float3 temp126;
    float3 temp127;
    temp127= saturate(r0.rgb);
    float3 temp128;
    float3 temp129;
    temp129= saturate(tex0.rgb);
    float3 temp130;
    float3 temp131;
    temp131= temp127 * temp129;
    temp125= temp131;
    float3 temp132;
    temp132= saturate(tex2.rgb);
    float3 temp133;
    float3 temp134;
    temp134= saturate(tex0.rgb);
    float3 temp135;
    float3 temp136;
    temp136= temp132 * temp134;
    temp126= temp136;
    temp124= temp125 + temp126;
    temp124= 2 * temp124;
    // exp.components=1 term=1
    // line 94: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp137;
    float temp138;
    temp138= saturate(r1.a);
    float3 temp139;
    float temp140;
    temp140= saturate(tex0.a);
    float3 temp141;
    float temp142;
    temp142= temp138 * temp140;
    temp137= temp142;
    temp137= 2 * temp137;
    // exp.components=1 term=1
    // line 95: T1a= EXPAND(T1a)
    float temp143;
    float temp144;
    temp144= 2 * saturate(tex1.a) - 1;
    float3 temp145;
    temp143= temp144;
    r0.rgb= temp124;
    r1.a= temp137;
    tex1.a= temp143;
    // exp.components=3 term=3
    // line 98: D0= OUT_SCALE_BY_2(D1*R1a)
    float3 temp146;
    float3 temp147;
    temp147= saturate(color1.rgb);
    float3 temp148;
    float temp149;
    temp149= saturate(r1.a);
    float3 temp150;
    float3 temp151;
    temp151= temp147 * temp149;
    temp146= temp151;
    temp146= 2 * temp146;
    // exp.components=1 term=1 term=1
    // line 100: T2a= 0 mux 1
    float temp152;
    float temp153;
    float temp154;
    float temp155;
    temp155= 0;
    float3 temp156;
    temp153= temp155;
    float temp157;
    temp157= 1;
    float3 temp158;
    temp154= temp157;
    temp152= lerp(temp153,temp154,round(r0.a));
    color0.rgb= temp146;
    tex2.a= temp152;
    // exp.components=3 term=3 term=3
    // line 102: SRCCOLOR= D0*INVERT(T1a) + [R0+D1]
    float3 temp159;
    float3 temp160;
    float3 temp161;
    float3 temp162;
    temp162= saturate(color0.rgb);
    float3 temp163;
    float temp164;
    temp164= 1-saturate(tex1.a);
    float3 temp165;
    float3 temp166;
    temp166= temp162 * temp164;
    temp160= temp166;
    float3 temp167;
    float3 temp168;
    temp168= r0.rgb+color1.rgb;
    temp167= temp168;
    temp161= temp167;
    temp159= temp160 + temp161;
    // exp.components=1 term=1
    // line 103: SRCALPHA= T2a
    float temp169;
    float temp170;
    temp170= saturate(tex2.a);
    float3 temp171;
    temp169= temp170;
    r0.rgb= temp159;
    r0.a= temp169;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
