; compiled from tags\rasterizer\pixel_shaders_dx9\shaders_shader_passes_lightmap_lightmap_tex_illum_alpha_test_3.txt
;
#const C5.rgb= $self_illum_color
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=1 term=1
    // line 25: R0= EXPAND(T2).EXPAND(T3)
    float temp0;
    float3 temp1;
    temp1= 2 * saturate(tex2.rgb) - 1;
    float3 temp2;
    float3 temp3;
    temp3= 2 * saturate(tex3.rgb) - 1;
    float3 temp4;
    float temp5;
    temp5= dot(temp1, temp3);
    temp0= temp5;
    // exp.components=1 term=1
    // line 26: R1= EXPAND(T2).EXPAND(D1)
    float temp6;
    float3 temp7;
    temp7= 2 * saturate(tex2.rgb) - 1;
    float3 temp8;
    float3 temp9;
    temp9= 2 * saturate(color1.rgb) - 1;
    float3 temp10;
    float temp11;
    temp11= dot(temp7, temp9);
    temp6= temp11;
    r0.rgb= temp0;
    r1.rgb= temp6;
    // exp.components=3 term=3 term=3
    // line 31: R0= R0*C0 + R1*C1
    float3 temp12;
    float3 temp13;
    float3 temp14;
    float3 temp15;
    temp15= saturate(r0.rgb);
    float3 temp16;
    float3 temp17;
    temp17= float3(0.501961,0.490196,0.470588);
    float3 temp18;
    float3 temp19;
    temp19= temp15 * temp17;
    temp13= temp19;
    float3 temp20;
    temp20= saturate(r1.rgb);
    float3 temp21;
    float3 temp22;
    temp22= float3(0.372549,0.352941,0.333333);
    float3 temp23;
    float3 temp24;
    temp24= temp20 * temp22;
    temp14= temp24;
    temp12= temp13 + temp14;
    // exp.components=1 term=1
    // line 32: R0a= T4a*T2a
    float temp25;
    float temp26;
    temp26= saturate(tex4.a);
    float3 temp27;
    float temp28;
    temp28= saturate(tex2.a);
    float3 temp29;
    float temp30;
    temp30= temp26 * temp28;
    temp25= temp30;
    r0.rgb= temp12;
    r0.a= temp25;
    // exp.components=1 term=1
    // line 36: R1= R0.C1a
    float temp31;
    float3 temp32;
    temp32= saturate(r0.rgb);
    float3 temp33;
    float temp34;
    temp34= 0.752941;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    r1.rgb= temp31;
    // exp.components=3 term=3 term=3
    // line 39: R0= OUT_SCALE_BY_2(R0*T0 + C0*T0)
    float3 temp37;
    float3 temp38;
    float3 temp39;
    float3 temp40;
    temp40= saturate(r0.rgb);
    float3 temp41;
    float3 temp42;
    temp42= saturate(tex0.rgb);
    float3 temp43;
    float3 temp44;
    temp44= temp40 * temp42;
    temp38= temp44;
    float3 temp45;
    temp45= float3(0.062745,0.062745,0.062745);
    float3 temp46;
    float3 temp47;
    temp47= saturate(tex0.rgb);
    float3 temp48;
    float3 temp49;
    temp49= temp45 * temp47;
    temp39= temp49;
    temp37= temp38 + temp39;
    temp37= 2 * temp37;
    // exp.components=1 term=1 term=1
    // line 40: T2a= 0 mux 1
    float temp50;
    float temp51;
    float temp52;
    float temp53;
    temp53= 0;
    float3 temp54;
    temp51= temp53;
    float temp55;
    temp55= 1;
    float3 temp56;
    temp52= temp55;
    temp50= lerp(temp51,temp52,round(r0.a));
    r0.rgb= temp37;
    tex2.a= temp50;
    // exp.components=3 term=3
    // line 43: T1= T1*C1
    float3 temp57;
    float3 temp58;
    temp58= saturate(tex1.rgb);
    float3 temp59;
    float3 temp60;
    temp60= c5.rgb;
    float3 temp61;
    float3 temp62;
    temp62= temp58 * temp60;
    temp57= temp62;
    tex1.rgb= temp57;
    // exp.components=3 term=3 term=3
    // line 45: R0= R0 + T1
    float3 temp63;
    float3 temp64;
    float3 temp65;
    float3 temp66;
    temp66= saturate(r0.rgb);
    float3 temp67;
    temp64= temp66;
    float3 temp68;
    temp68= saturate(tex1.rgb);
    float3 temp69;
    temp65= temp68;
    temp63= temp64 + temp65;
    r0.rgb= temp63;
    // exp.components=3 term=3
    // line 47: SRCCOLOR= R0
    float3 temp70;
    float3 temp71;
    temp71= saturate(r0.rgb);
    float3 temp72;
    temp70= temp71;
    // exp.components=1 term=1
    // line 48: SRCALPHA= T2a
    float temp73;
    float temp74;
    temp74= saturate(tex2.a);
    float3 temp75;
    temp73= temp74;
    r0.rgb= temp70;
    r0.a= temp73;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
