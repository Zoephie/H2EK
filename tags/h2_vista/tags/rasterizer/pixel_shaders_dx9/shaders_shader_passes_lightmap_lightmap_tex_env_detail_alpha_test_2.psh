; compiled from tags\rasterizer\pixel_shaders_dx9\shaders_shader_passes_lightmap_lightmap_tex_env_detail_alpha_test_2.txt
;
#const C2.rgb= $lightmap_direct_color
#const C3.rgb= $lightmap_indirect_color
#const C4.rgb= $specular_color
#const C5.rgb= $specular_glancing_color
#const C6.rgb= $lightmap_specular_color
#const C6.a= $lightmap_object_env_brightness
#const C7.rgb= $env_tint_color
#const C8.rgb= $env_glancing_tint_color
#const C9.rgb= $lightmap_ambient_color
#const C10.a= $env_brightness
#const C11.a= $env_glancing_brightness
#case $specular_type=0
sampler2D s0 : register(S0);
samplerCUBE s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
sampler2D s5 : register(S5);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 c8 : register(C8);
float4 c9 : register(C9);
float4 c10 : register(C10);
float4 c11 : register(C11);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
#ifdef CENTROID5
    float4 tc5 : TEXCOORD5_centroid,
#else
    float4 tc5 : TEXCOORD5,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 2 dot 0 input-stage 0
    float4 tex1= texCUBE(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
// texture stage 5 mode 0 dot 0 input-stage 0
    float4 tex5= tex2Dproj(s5, tc5);
    // exp.components=3 term=3
    // line 27: T0= OUT_SCALE_BY_2(T0*T4)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex4.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 28: T0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    tex0.rgb= temp0;
    tex0.a= temp6;
    // exp.components=1 term=1
    // line 30: R0a= T5a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex5.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    r0.a= temp12;
    // exp.components=1 term=1 term=1
    // line 32: T2a= 0 mux 1
    float temp18;
    float temp19;
    float temp20;
    float temp21;
    temp21= 0;
    float3 temp22;
    temp19= temp21;
    float temp23;
    temp23= 1;
    float3 temp24;
    temp20= temp23;
    temp18= lerp(temp19,temp20,round(r0.a));
    tex2.a= temp18;
    // exp.components=1 term=1
    // line 35: R0= EXPAND(T2).EXPAND(D0)
    float temp25;
    float3 temp26;
    temp26= 2 * saturate(tex2.rgb) - 1;
    float3 temp27;
    float3 temp28;
    temp28= 2 * saturate(color0.rgb) - 1;
    float3 temp29;
    float temp30;
    temp30= dot(temp26, temp28);
    temp25= temp30;
    // exp.components=1 term=1
    // line 36: R1= EXPAND(T3).EXPAND(D0)
    float temp31;
    float3 temp32;
    temp32= 2 * saturate(tex3.rgb) - 1;
    float3 temp33;
    float3 temp34;
    temp34= 2 * saturate(color0.rgb) - 1;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    // exp.components=1 term=1
    // line 37: T3a= C0a
    float temp37;
    float temp38;
    temp38= 0.752941;
    float3 temp39;
    temp37= temp38;
    r0.rgb= temp25;
    r1.rgb= temp31;
    tex3.a= temp37;
    // exp.components=1 term=1
    // line 40: T2= EXPAND(T2).EXPAND(T3)
    float temp40;
    float3 temp41;
    temp41= 2 * saturate(tex2.rgb) - 1;
    float3 temp42;
    float3 temp43;
    temp43= 2 * saturate(tex3.rgb) - 1;
    float3 temp44;
    float temp45;
    temp45= dot(temp41, temp43);
    temp40= temp45;
    // exp.components=1 term=1
    // line 41: T3= EXPAND(T2).EXPAND(D1)
    float temp46;
    float3 temp47;
    temp47= 2 * saturate(tex2.rgb) - 1;
    float3 temp48;
    float3 temp49;
    temp49= 2 * saturate(color1.rgb) - 1;
    float3 temp50;
    float temp51;
    temp51= dot(temp47, temp49);
    temp46= temp51;
    // exp.components=1 term=1
    // line 42: R0a= R0b
    float temp52;
    float temp53;
    temp53= saturate(r0.b);
    float3 temp54;
    temp52= temp53;
    tex2.rgb= temp40;
    tex3.rgb= temp46;
    r0.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: R0= T2*C0 + T3*C1
    float3 temp55;
    float3 temp56;
    float3 temp57;
    float3 temp58;
    temp58= saturate(tex2.rgb);
    float3 temp59;
    float3 temp60;
    temp60= c2.rgb;
    float3 temp61;
    float3 temp62;
    temp62= temp58 * temp60;
    temp56= temp62;
    float3 temp63;
    temp63= saturate(tex3.rgb);
    float3 temp64;
    float3 temp65;
    temp65= c3.rgb;
    float3 temp66;
    float3 temp67;
    temp67= temp63 * temp65;
    temp57= temp67;
    temp55= temp56 + temp57;
    // exp.components=1 term=1 term=1
    // line 48: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp68;
    float temp69;
    float temp70;
    float temp71;
    temp71= tex2.b;
    float3 temp72;
    float temp73;
    temp73= r0.b;
    float3 temp74;
    float temp75;
    temp75= temp71 * temp73;
    temp69= temp75;
    float temp76;
    temp76= 0.5;
    float3 temp77;
    float temp78;
    temp78= r1.b;
    float3 temp79;
    float temp80;
    temp80= temp76 * temp78;
    temp70= temp80;
    temp68= temp69 - temp70;
    temp68= 2 * temp68;
    r0.rgb= temp55;
    r1.a= temp68;
    // exp.components=3 term=3 term=3
    // line 53: D1= R0a*C0 + INVERT(R0a)*C1
    float3 temp81;
    float3 temp82;
    float3 temp83;
    float temp84;
    temp84= saturate(r0.a);
    float3 temp85;
    float3 temp86;
    temp86= c4.rgb;
    float3 temp87;
    float3 temp88;
    temp88= temp84 * temp86;
    temp82= temp88;
    float temp89;
    temp89= 1-saturate(r0.a);
    float3 temp90;
    float3 temp91;
    temp91= c5.rgb;
    float3 temp92;
    float3 temp93;
    temp93= temp89 * temp91;
    temp83= temp93;
    temp81= temp82 + temp83;
    // exp.components=1 term=1
    // line 54: D0a= R1a*R1a
    float temp94;
    float temp95;
    temp95= saturate(r1.a);
    float3 temp96;
    float temp97;
    temp97= saturate(r1.a);
    float3 temp98;
    float temp99;
    temp99= temp95 * temp97;
    temp94= temp99;
    color1.rgb= temp81;
    color0.a= temp94;
    // exp.components=1 term=1
    // line 58: R1= C0a
    float temp100;
    float temp101;
    temp101= c6.a;
    float3 temp102;
    temp100= temp101;
    // exp.components=3 term=3
    // line 60: D1= D1*C0
    float3 temp103;
    float3 temp104;
    temp104= saturate(color1.rgb);
    float3 temp105;
    float3 temp106;
    temp106= c6.rgb;
    float3 temp107;
    float3 temp108;
    temp108= temp104 * temp106;
    temp103= temp108;
    // exp.components=1 term=1
    // line 61: R1a= D0a*D0a
    float temp109;
    float temp110;
    temp110= saturate(color0.a);
    float3 temp111;
    float temp112;
    temp112= saturate(color0.a);
    float3 temp113;
    float temp114;
    temp114= temp110 * temp112;
    temp109= temp114;
    r1.rgb= temp100;
    color1.rgb= temp103;
    r1.a= temp109;
    // exp.components=3 term=3 term=3
    // line 68: D0= OUT_SCALE_BY_1/2(R0a*C0 + INVERT(R0a)*C1)
    float3 temp115;
    float3 temp116;
    float3 temp117;
    float temp118;
    temp118= saturate(r0.a);
    float3 temp119;
    float3 temp120;
    temp120= c7.rgb;
    float3 temp121;
    float3 temp122;
    temp122= temp118 * temp120;
    temp116= temp122;
    float temp123;
    temp123= 1-saturate(r0.a);
    float3 temp124;
    float3 temp125;
    temp125= c8.rgb;
    float3 temp126;
    float3 temp127;
    temp127= temp123 * temp125;
    temp117= temp127;
    temp115= temp116 + temp117;
    temp115= 0.5 * temp115;
    // exp.components=1 term=1
    // line 69: R1a= 0
    float temp128;
    float temp129;
    temp129= 0;
    float3 temp130;
    temp128= temp129;
    color0.rgb= temp115;
    r1.a= temp128;
    // exp.components=3 term=3 term=3
    // line 92: R0= OUT_SCALE_BY_2(R0*T0 + C0*T0)
    float3 temp131;
    float3 temp132;
    float3 temp133;
    float3 temp134;
    temp134= saturate(r0.rgb);
    float3 temp135;
    float3 temp136;
    temp136= saturate(tex0.rgb);
    float3 temp137;
    float3 temp138;
    temp138= temp134 * temp136;
    temp132= temp138;
    float3 temp139;
    temp139= c9.rgb;
    float3 temp140;
    float3 temp141;
    temp141= saturate(tex0.rgb);
    float3 temp142;
    float3 temp143;
    temp143= temp139 * temp141;
    temp133= temp143;
    temp131= temp132 + temp133;
    temp131= 2 * temp131;
    // exp.components=1 term=1
    // line 93: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp144;
    float temp145;
    temp145= saturate(r1.a);
    float3 temp146;
    float temp147;
    temp147= saturate(tex0.a);
    float3 temp148;
    float temp149;
    temp149= temp145 * temp147;
    temp144= temp149;
    temp144= 2 * temp144;
    // exp.components=1 term=1
    // line 94: D0a= OUT_SCALE_BY_2(R1b*T0a)
    float temp150;
    float temp151;
    temp151= saturate(r1.b);
    float3 temp152;
    float temp153;
    temp153= saturate(tex0.a);
    float3 temp154;
    float temp155;
    temp155= temp151 * temp153;
    temp150= temp155;
    temp150= 2 * temp150;
    r0.rgb= temp131;
    r1.a= temp144;
    color0.a= temp150;
    // exp.components=3 term=3
    // line 99: D0= D0*T1
    float3 temp156;
    float3 temp157;
    temp157= saturate(color0.rgb);
    float3 temp158;
    float3 temp159;
    temp159= saturate(tex1.rgb);
    float3 temp160;
    float3 temp161;
    temp161= temp157 * temp159;
    temp156= temp161;
    // exp.components=3 term=3
    // line 100: D1= D1*R1a
    float3 temp162;
    float3 temp163;
    temp163= saturate(color1.rgb);
    float3 temp164;
    float temp165;
    temp165= saturate(r1.a);
    float3 temp166;
    float3 temp167;
    temp167= temp163 * temp165;
    temp162= temp167;
    // exp.components=1 term=1 term=1
    // line 101: R0a= R0a*C0a + INVERT(R0a)*C1a
    float temp168;
    float temp169;
    float temp170;
    float temp171;
    temp171= saturate(r0.a);
    float3 temp172;
    float temp173;
    temp173= c10.a;
    float3 temp174;
    float temp175;
    temp175= temp171 * temp173;
    temp169= temp175;
    float temp176;
    temp176= 1-saturate(r0.a);
    float3 temp177;
    float temp178;
    temp178= c11.a;
    float3 temp179;
    float temp180;
    temp180= temp176 * temp178;
    temp170= temp180;
    temp168= temp169 + temp170;
    color0.rgb= temp156;
    color1.rgb= temp162;
    r0.a= temp168;
    // exp.components=3 term=3
    // line 104: E= D0
    float3 temp181;
    temp181= saturate(color0.rgb);
    float3 temp182;
    e= temp181;
    // exp.components=1 term=1
    // line 105: F= D0a
    float temp183;
    temp183= saturate(color0.a);
    float3 temp184;
    f= temp183;
    // exp.components=3 term=3 term=3
    // line 106: SRCCOLOR= EF*R0a + [R0+D1]
    float3 temp185;
    float3 temp186;
    float3 temp187;
    float3 temp188;
    float3 temp189;
    temp189= e.rgb*f.rgb;
    temp188= temp189;
    float temp190;
    temp190= saturate(r0.a);
    float3 temp191;
    float3 temp192;
    temp192= temp188 * temp190;
    temp186= temp192;
    float3 temp193;
    float3 temp194;
    temp194= r0.rgb+color1.rgb;
    temp193= temp194;
    temp187= temp193;
    temp185= temp186 + temp187;
    // exp.components=1 term=1
    // line 107: SRCALPHA= T2a
    float temp195;
    float temp196;
    temp196= saturate(tex2.a);
    float3 temp197;
    temp195= temp196;
    r0.rgb= temp185;
    r0.a= temp195;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=1
sampler2D s0 : register(S0);
samplerCUBE s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
sampler2D s5 : register(S5);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 c8 : register(C8);
float4 c9 : register(C9);
float4 c10 : register(C10);
float4 c11 : register(C11);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
#ifdef CENTROID5
    float4 tc5 : TEXCOORD5_centroid,
#else
    float4 tc5 : TEXCOORD5,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 2 dot 0 input-stage 0
    float4 tex1= texCUBE(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
// texture stage 5 mode 0 dot 0 input-stage 0
    float4 tex5= tex2Dproj(s5, tc5);
    // exp.components=3 term=3
    // line 27: T0= OUT_SCALE_BY_2(T0*T4)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex4.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 28: T0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    tex0.rgb= temp0;
    tex0.a= temp6;
    // exp.components=1 term=1
    // line 30: R0a= T5a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex5.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    r0.a= temp12;
    // exp.components=1 term=1 term=1
    // line 32: T2a= 0 mux 1
    float temp18;
    float temp19;
    float temp20;
    float temp21;
    temp21= 0;
    float3 temp22;
    temp19= temp21;
    float temp23;
    temp23= 1;
    float3 temp24;
    temp20= temp23;
    temp18= lerp(temp19,temp20,round(r0.a));
    tex2.a= temp18;
    // exp.components=1 term=1
    // line 35: R0= EXPAND(T2).EXPAND(D0)
    float temp25;
    float3 temp26;
    temp26= 2 * saturate(tex2.rgb) - 1;
    float3 temp27;
    float3 temp28;
    temp28= 2 * saturate(color0.rgb) - 1;
    float3 temp29;
    float temp30;
    temp30= dot(temp26, temp28);
    temp25= temp30;
    // exp.components=1 term=1
    // line 36: R1= EXPAND(T3).EXPAND(D0)
    float temp31;
    float3 temp32;
    temp32= 2 * saturate(tex3.rgb) - 1;
    float3 temp33;
    float3 temp34;
    temp34= 2 * saturate(color0.rgb) - 1;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    // exp.components=1 term=1
    // line 37: T3a= C0a
    float temp37;
    float temp38;
    temp38= 0.752941;
    float3 temp39;
    temp37= temp38;
    r0.rgb= temp25;
    r1.rgb= temp31;
    tex3.a= temp37;
    // exp.components=1 term=1
    // line 40: T2= EXPAND(T2).EXPAND(T3)
    float temp40;
    float3 temp41;
    temp41= 2 * saturate(tex2.rgb) - 1;
    float3 temp42;
    float3 temp43;
    temp43= 2 * saturate(tex3.rgb) - 1;
    float3 temp44;
    float temp45;
    temp45= dot(temp41, temp43);
    temp40= temp45;
    // exp.components=1 term=1
    // line 41: T3= EXPAND(T2).EXPAND(D1)
    float temp46;
    float3 temp47;
    temp47= 2 * saturate(tex2.rgb) - 1;
    float3 temp48;
    float3 temp49;
    temp49= 2 * saturate(color1.rgb) - 1;
    float3 temp50;
    float temp51;
    temp51= dot(temp47, temp49);
    temp46= temp51;
    // exp.components=1 term=1
    // line 42: R0a= R0b
    float temp52;
    float temp53;
    temp53= saturate(r0.b);
    float3 temp54;
    temp52= temp53;
    tex2.rgb= temp40;
    tex3.rgb= temp46;
    r0.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: R0= T2*C0 + T3*C1
    float3 temp55;
    float3 temp56;
    float3 temp57;
    float3 temp58;
    temp58= saturate(tex2.rgb);
    float3 temp59;
    float3 temp60;
    temp60= c2.rgb;
    float3 temp61;
    float3 temp62;
    temp62= temp58 * temp60;
    temp56= temp62;
    float3 temp63;
    temp63= saturate(tex3.rgb);
    float3 temp64;
    float3 temp65;
    temp65= c3.rgb;
    float3 temp66;
    float3 temp67;
    temp67= temp63 * temp65;
    temp57= temp67;
    temp55= temp56 + temp57;
    // exp.components=1 term=1 term=1
    // line 48: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp68;
    float temp69;
    float temp70;
    float temp71;
    temp71= tex2.b;
    float3 temp72;
    float temp73;
    temp73= r0.b;
    float3 temp74;
    float temp75;
    temp75= temp71 * temp73;
    temp69= temp75;
    float temp76;
    temp76= 0.5;
    float3 temp77;
    float temp78;
    temp78= r1.b;
    float3 temp79;
    float temp80;
    temp80= temp76 * temp78;
    temp70= temp80;
    temp68= temp69 - temp70;
    temp68= 2 * temp68;
    r0.rgb= temp55;
    r1.a= temp68;
    // exp.components=3 term=3 term=3
    // line 53: D1= R0a*C0 + INVERT(R0a)*C1
    float3 temp81;
    float3 temp82;
    float3 temp83;
    float temp84;
    temp84= saturate(r0.a);
    float3 temp85;
    float3 temp86;
    temp86= c4.rgb;
    float3 temp87;
    float3 temp88;
    temp88= temp84 * temp86;
    temp82= temp88;
    float temp89;
    temp89= 1-saturate(r0.a);
    float3 temp90;
    float3 temp91;
    temp91= c5.rgb;
    float3 temp92;
    float3 temp93;
    temp93= temp89 * temp91;
    temp83= temp93;
    temp81= temp82 + temp83;
    // exp.components=1 term=1
    // line 54: D0a= R1a*R1a
    float temp94;
    float temp95;
    temp95= saturate(r1.a);
    float3 temp96;
    float temp97;
    temp97= saturate(r1.a);
    float3 temp98;
    float temp99;
    temp99= temp95 * temp97;
    temp94= temp99;
    color1.rgb= temp81;
    color0.a= temp94;
    // exp.components=1 term=1
    // line 58: R1= C0a
    float temp100;
    float temp101;
    temp101= c6.a;
    float3 temp102;
    temp100= temp101;
    // exp.components=3 term=3
    // line 60: D1= D1*C0
    float3 temp103;
    float3 temp104;
    temp104= saturate(color1.rgb);
    float3 temp105;
    float3 temp106;
    temp106= c6.rgb;
    float3 temp107;
    float3 temp108;
    temp108= temp104 * temp106;
    temp103= temp108;
    // exp.components=1 term=1
    // line 61: R1a= D0a*D0a
    float temp109;
    float temp110;
    temp110= saturate(color0.a);
    float3 temp111;
    float temp112;
    temp112= saturate(color0.a);
    float3 temp113;
    float temp114;
    temp114= temp110 * temp112;
    temp109= temp114;
    r1.rgb= temp100;
    color1.rgb= temp103;
    r1.a= temp109;
    // exp.components=3 term=3 term=3
    // line 74: D0= OUT_SCALE_BY_1/2(R0a*C0 + INVERT(R0a)*C1)
    float3 temp115;
    float3 temp116;
    float3 temp117;
    float temp118;
    temp118= saturate(r0.a);
    float3 temp119;
    float3 temp120;
    temp120= c7.rgb;
    float3 temp121;
    float3 temp122;
    temp122= temp118 * temp120;
    temp116= temp122;
    float temp123;
    temp123= 1-saturate(r0.a);
    float3 temp124;
    float3 temp125;
    temp125= c8.rgb;
    float3 temp126;
    float3 temp127;
    temp127= temp123 * temp125;
    temp117= temp127;
    temp115= temp116 + temp117;
    temp115= 0.5 * temp115;
    // exp.components=1 term=1
    // line 75: R1a= R1a*R1a
    float temp128;
    float temp129;
    temp129= saturate(r1.a);
    float3 temp130;
    float temp131;
    temp131= saturate(r1.a);
    float3 temp132;
    float temp133;
    temp133= temp129 * temp131;
    temp128= temp133;
    color0.rgb= temp115;
    r1.a= temp128;
    // exp.components=3 term=3 term=3
    // line 92: R0= OUT_SCALE_BY_2(R0*T0 + C0*T0)
    float3 temp134;
    float3 temp135;
    float3 temp136;
    float3 temp137;
    temp137= saturate(r0.rgb);
    float3 temp138;
    float3 temp139;
    temp139= saturate(tex0.rgb);
    float3 temp140;
    float3 temp141;
    temp141= temp137 * temp139;
    temp135= temp141;
    float3 temp142;
    temp142= c9.rgb;
    float3 temp143;
    float3 temp144;
    temp144= saturate(tex0.rgb);
    float3 temp145;
    float3 temp146;
    temp146= temp142 * temp144;
    temp136= temp146;
    temp134= temp135 + temp136;
    temp134= 2 * temp134;
    // exp.components=1 term=1
    // line 93: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp147;
    float temp148;
    temp148= saturate(r1.a);
    float3 temp149;
    float temp150;
    temp150= saturate(tex0.a);
    float3 temp151;
    float temp152;
    temp152= temp148 * temp150;
    temp147= temp152;
    temp147= 2 * temp147;
    // exp.components=1 term=1
    // line 94: D0a= OUT_SCALE_BY_2(R1b*T0a)
    float temp153;
    float temp154;
    temp154= saturate(r1.b);
    float3 temp155;
    float temp156;
    temp156= saturate(tex0.a);
    float3 temp157;
    float temp158;
    temp158= temp154 * temp156;
    temp153= temp158;
    temp153= 2 * temp153;
    r0.rgb= temp134;
    r1.a= temp147;
    color0.a= temp153;
    // exp.components=3 term=3
    // line 99: D0= D0*T1
    float3 temp159;
    float3 temp160;
    temp160= saturate(color0.rgb);
    float3 temp161;
    float3 temp162;
    temp162= saturate(tex1.rgb);
    float3 temp163;
    float3 temp164;
    temp164= temp160 * temp162;
    temp159= temp164;
    // exp.components=3 term=3
    // line 100: D1= D1*R1a
    float3 temp165;
    float3 temp166;
    temp166= saturate(color1.rgb);
    float3 temp167;
    float temp168;
    temp168= saturate(r1.a);
    float3 temp169;
    float3 temp170;
    temp170= temp166 * temp168;
    temp165= temp170;
    // exp.components=1 term=1 term=1
    // line 101: R0a= R0a*C0a + INVERT(R0a)*C1a
    float temp171;
    float temp172;
    float temp173;
    float temp174;
    temp174= saturate(r0.a);
    float3 temp175;
    float temp176;
    temp176= c10.a;
    float3 temp177;
    float temp178;
    temp178= temp174 * temp176;
    temp172= temp178;
    float temp179;
    temp179= 1-saturate(r0.a);
    float3 temp180;
    float temp181;
    temp181= c11.a;
    float3 temp182;
    float temp183;
    temp183= temp179 * temp181;
    temp173= temp183;
    temp171= temp172 + temp173;
    color0.rgb= temp159;
    color1.rgb= temp165;
    r0.a= temp171;
    // exp.components=3 term=3
    // line 104: E= D0
    float3 temp184;
    temp184= saturate(color0.rgb);
    float3 temp185;
    e= temp184;
    // exp.components=1 term=1
    // line 105: F= D0a
    float temp186;
    temp186= saturate(color0.a);
    float3 temp187;
    f= temp186;
    // exp.components=3 term=3 term=3
    // line 106: SRCCOLOR= EF*R0a + [R0+D1]
    float3 temp188;
    float3 temp189;
    float3 temp190;
    float3 temp191;
    float3 temp192;
    temp192= e.rgb*f.rgb;
    temp191= temp192;
    float temp193;
    temp193= saturate(r0.a);
    float3 temp194;
    float3 temp195;
    temp195= temp191 * temp193;
    temp189= temp195;
    float3 temp196;
    float3 temp197;
    temp197= r0.rgb+color1.rgb;
    temp196= temp197;
    temp190= temp196;
    temp188= temp189 + temp190;
    // exp.components=1 term=1
    // line 107: SRCALPHA= T2a
    float temp198;
    float temp199;
    temp199= saturate(tex2.a);
    float3 temp200;
    temp198= temp199;
    r0.rgb= temp188;
    r0.a= temp198;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=2
sampler2D s0 : register(S0);
samplerCUBE s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
sampler2D s5 : register(S5);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 c8 : register(C8);
float4 c9 : register(C9);
float4 c10 : register(C10);
float4 c11 : register(C11);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
#ifdef CENTROID5
    float4 tc5 : TEXCOORD5_centroid,
#else
    float4 tc5 : TEXCOORD5,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 2 dot 0 input-stage 0
    float4 tex1= texCUBE(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
// texture stage 5 mode 0 dot 0 input-stage 0
    float4 tex5= tex2Dproj(s5, tc5);
    // exp.components=3 term=3
    // line 27: T0= OUT_SCALE_BY_2(T0*T4)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex4.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 28: T0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    tex0.rgb= temp0;
    tex0.a= temp6;
    // exp.components=1 term=1
    // line 30: R0a= T5a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex5.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    r0.a= temp12;
    // exp.components=1 term=1 term=1
    // line 32: T2a= 0 mux 1
    float temp18;
    float temp19;
    float temp20;
    float temp21;
    temp21= 0;
    float3 temp22;
    temp19= temp21;
    float temp23;
    temp23= 1;
    float3 temp24;
    temp20= temp23;
    temp18= lerp(temp19,temp20,round(r0.a));
    tex2.a= temp18;
    // exp.components=1 term=1
    // line 35: R0= EXPAND(T2).EXPAND(D0)
    float temp25;
    float3 temp26;
    temp26= 2 * saturate(tex2.rgb) - 1;
    float3 temp27;
    float3 temp28;
    temp28= 2 * saturate(color0.rgb) - 1;
    float3 temp29;
    float temp30;
    temp30= dot(temp26, temp28);
    temp25= temp30;
    // exp.components=1 term=1
    // line 36: R1= EXPAND(T3).EXPAND(D0)
    float temp31;
    float3 temp32;
    temp32= 2 * saturate(tex3.rgb) - 1;
    float3 temp33;
    float3 temp34;
    temp34= 2 * saturate(color0.rgb) - 1;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    // exp.components=1 term=1
    // line 37: T3a= C0a
    float temp37;
    float temp38;
    temp38= 0.752941;
    float3 temp39;
    temp37= temp38;
    r0.rgb= temp25;
    r1.rgb= temp31;
    tex3.a= temp37;
    // exp.components=1 term=1
    // line 40: T2= EXPAND(T2).EXPAND(T3)
    float temp40;
    float3 temp41;
    temp41= 2 * saturate(tex2.rgb) - 1;
    float3 temp42;
    float3 temp43;
    temp43= 2 * saturate(tex3.rgb) - 1;
    float3 temp44;
    float temp45;
    temp45= dot(temp41, temp43);
    temp40= temp45;
    // exp.components=1 term=1
    // line 41: T3= EXPAND(T2).EXPAND(D1)
    float temp46;
    float3 temp47;
    temp47= 2 * saturate(tex2.rgb) - 1;
    float3 temp48;
    float3 temp49;
    temp49= 2 * saturate(color1.rgb) - 1;
    float3 temp50;
    float temp51;
    temp51= dot(temp47, temp49);
    temp46= temp51;
    // exp.components=1 term=1
    // line 42: R0a= R0b
    float temp52;
    float temp53;
    temp53= saturate(r0.b);
    float3 temp54;
    temp52= temp53;
    tex2.rgb= temp40;
    tex3.rgb= temp46;
    r0.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: R0= T2*C0 + T3*C1
    float3 temp55;
    float3 temp56;
    float3 temp57;
    float3 temp58;
    temp58= saturate(tex2.rgb);
    float3 temp59;
    float3 temp60;
    temp60= c2.rgb;
    float3 temp61;
    float3 temp62;
    temp62= temp58 * temp60;
    temp56= temp62;
    float3 temp63;
    temp63= saturate(tex3.rgb);
    float3 temp64;
    float3 temp65;
    temp65= c3.rgb;
    float3 temp66;
    float3 temp67;
    temp67= temp63 * temp65;
    temp57= temp67;
    temp55= temp56 + temp57;
    // exp.components=1 term=1 term=1
    // line 48: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp68;
    float temp69;
    float temp70;
    float temp71;
    temp71= tex2.b;
    float3 temp72;
    float temp73;
    temp73= r0.b;
    float3 temp74;
    float temp75;
    temp75= temp71 * temp73;
    temp69= temp75;
    float temp76;
    temp76= 0.5;
    float3 temp77;
    float temp78;
    temp78= r1.b;
    float3 temp79;
    float temp80;
    temp80= temp76 * temp78;
    temp70= temp80;
    temp68= temp69 - temp70;
    temp68= 2 * temp68;
    r0.rgb= temp55;
    r1.a= temp68;
    // exp.components=3 term=3 term=3
    // line 53: D1= R0a*C0 + INVERT(R0a)*C1
    float3 temp81;
    float3 temp82;
    float3 temp83;
    float temp84;
    temp84= saturate(r0.a);
    float3 temp85;
    float3 temp86;
    temp86= c4.rgb;
    float3 temp87;
    float3 temp88;
    temp88= temp84 * temp86;
    temp82= temp88;
    float temp89;
    temp89= 1-saturate(r0.a);
    float3 temp90;
    float3 temp91;
    temp91= c5.rgb;
    float3 temp92;
    float3 temp93;
    temp93= temp89 * temp91;
    temp83= temp93;
    temp81= temp82 + temp83;
    // exp.components=1 term=1
    // line 54: D0a= R1a*R1a
    float temp94;
    float temp95;
    temp95= saturate(r1.a);
    float3 temp96;
    float temp97;
    temp97= saturate(r1.a);
    float3 temp98;
    float temp99;
    temp99= temp95 * temp97;
    temp94= temp99;
    color1.rgb= temp81;
    color0.a= temp94;
    // exp.components=1 term=1
    // line 58: R1= C0a
    float temp100;
    float temp101;
    temp101= c6.a;
    float3 temp102;
    temp100= temp101;
    // exp.components=3 term=3
    // line 60: D1= D1*C0
    float3 temp103;
    float3 temp104;
    temp104= saturate(color1.rgb);
    float3 temp105;
    float3 temp106;
    temp106= c6.rgb;
    float3 temp107;
    float3 temp108;
    temp108= temp104 * temp106;
    temp103= temp108;
    // exp.components=1 term=1
    // line 61: R1a= D0a*D0a
    float temp109;
    float temp110;
    temp110= saturate(color0.a);
    float3 temp111;
    float temp112;
    temp112= saturate(color0.a);
    float3 temp113;
    float temp114;
    temp114= temp110 * temp112;
    temp109= temp114;
    r1.rgb= temp100;
    color1.rgb= temp103;
    r1.a= temp109;
    // exp.components=3 term=3 term=3
    // line 80: D0= OUT_SCALE_BY_1/2(R0a*C0 + INVERT(R0a)*C1)
    float3 temp115;
    float3 temp116;
    float3 temp117;
    float temp118;
    temp118= saturate(r0.a);
    float3 temp119;
    float3 temp120;
    temp120= c7.rgb;
    float3 temp121;
    float3 temp122;
    temp122= temp118 * temp120;
    temp116= temp122;
    float temp123;
    temp123= 1-saturate(r0.a);
    float3 temp124;
    float3 temp125;
    temp125= c8.rgb;
    float3 temp126;
    float3 temp127;
    temp127= temp123 * temp125;
    temp117= temp127;
    temp115= temp116 + temp117;
    temp115= 0.5 * temp115;
    // exp.components=1 term=1
    // line 81: R1a= D0a
    float temp128;
    float temp129;
    temp129= saturate(color0.a);
    float3 temp130;
    temp128= temp129;
    color0.rgb= temp115;
    r1.a= temp128;
    // exp.components=3 term=3 term=3
    // line 92: R0= OUT_SCALE_BY_2(R0*T0 + C0*T0)
    float3 temp131;
    float3 temp132;
    float3 temp133;
    float3 temp134;
    temp134= saturate(r0.rgb);
    float3 temp135;
    float3 temp136;
    temp136= saturate(tex0.rgb);
    float3 temp137;
    float3 temp138;
    temp138= temp134 * temp136;
    temp132= temp138;
    float3 temp139;
    temp139= c9.rgb;
    float3 temp140;
    float3 temp141;
    temp141= saturate(tex0.rgb);
    float3 temp142;
    float3 temp143;
    temp143= temp139 * temp141;
    temp133= temp143;
    temp131= temp132 + temp133;
    temp131= 2 * temp131;
    // exp.components=1 term=1
    // line 93: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp144;
    float temp145;
    temp145= saturate(r1.a);
    float3 temp146;
    float temp147;
    temp147= saturate(tex0.a);
    float3 temp148;
    float temp149;
    temp149= temp145 * temp147;
    temp144= temp149;
    temp144= 2 * temp144;
    // exp.components=1 term=1
    // line 94: D0a= OUT_SCALE_BY_2(R1b*T0a)
    float temp150;
    float temp151;
    temp151= saturate(r1.b);
    float3 temp152;
    float temp153;
    temp153= saturate(tex0.a);
    float3 temp154;
    float temp155;
    temp155= temp151 * temp153;
    temp150= temp155;
    temp150= 2 * temp150;
    r0.rgb= temp131;
    r1.a= temp144;
    color0.a= temp150;
    // exp.components=3 term=3
    // line 99: D0= D0*T1
    float3 temp156;
    float3 temp157;
    temp157= saturate(color0.rgb);
    float3 temp158;
    float3 temp159;
    temp159= saturate(tex1.rgb);
    float3 temp160;
    float3 temp161;
    temp161= temp157 * temp159;
    temp156= temp161;
    // exp.components=3 term=3
    // line 100: D1= D1*R1a
    float3 temp162;
    float3 temp163;
    temp163= saturate(color1.rgb);
    float3 temp164;
    float temp165;
    temp165= saturate(r1.a);
    float3 temp166;
    float3 temp167;
    temp167= temp163 * temp165;
    temp162= temp167;
    // exp.components=1 term=1 term=1
    // line 101: R0a= R0a*C0a + INVERT(R0a)*C1a
    float temp168;
    float temp169;
    float temp170;
    float temp171;
    temp171= saturate(r0.a);
    float3 temp172;
    float temp173;
    temp173= c10.a;
    float3 temp174;
    float temp175;
    temp175= temp171 * temp173;
    temp169= temp175;
    float temp176;
    temp176= 1-saturate(r0.a);
    float3 temp177;
    float temp178;
    temp178= c11.a;
    float3 temp179;
    float temp180;
    temp180= temp176 * temp178;
    temp170= temp180;
    temp168= temp169 + temp170;
    color0.rgb= temp156;
    color1.rgb= temp162;
    r0.a= temp168;
    // exp.components=3 term=3
    // line 104: E= D0
    float3 temp181;
    temp181= saturate(color0.rgb);
    float3 temp182;
    e= temp181;
    // exp.components=1 term=1
    // line 105: F= D0a
    float temp183;
    temp183= saturate(color0.a);
    float3 temp184;
    f= temp183;
    // exp.components=3 term=3 term=3
    // line 106: SRCCOLOR= EF*R0a + [R0+D1]
    float3 temp185;
    float3 temp186;
    float3 temp187;
    float3 temp188;
    float3 temp189;
    temp189= e.rgb*f.rgb;
    temp188= temp189;
    float temp190;
    temp190= saturate(r0.a);
    float3 temp191;
    float3 temp192;
    temp192= temp188 * temp190;
    temp186= temp192;
    float3 temp193;
    float3 temp194;
    temp194= r0.rgb+color1.rgb;
    temp193= temp194;
    temp187= temp193;
    temp185= temp186 + temp187;
    // exp.components=1 term=1
    // line 107: SRCALPHA= T2a
    float temp195;
    float temp196;
    temp196= saturate(tex2.a);
    float3 temp197;
    temp195= temp196;
    r0.rgb= temp185;
    r0.a= temp195;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=3
sampler2D s0 : register(S0);
samplerCUBE s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
sampler2D s5 : register(S5);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 c8 : register(C8);
float4 c9 : register(C9);
float4 c10 : register(C10);
float4 c11 : register(C11);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
#ifdef CENTROID5
    float4 tc5 : TEXCOORD5_centroid,
#else
    float4 tc5 : TEXCOORD5,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 2 dot 0 input-stage 0
    float4 tex1= texCUBE(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
// texture stage 5 mode 0 dot 0 input-stage 0
    float4 tex5= tex2Dproj(s5, tc5);
    // exp.components=3 term=3
    // line 27: T0= OUT_SCALE_BY_2(T0*T4)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex4.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 28: T0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    tex0.rgb= temp0;
    tex0.a= temp6;
    // exp.components=1 term=1
    // line 30: R0a= T5a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex5.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    r0.a= temp12;
    // exp.components=1 term=1 term=1
    // line 32: T2a= 0 mux 1
    float temp18;
    float temp19;
    float temp20;
    float temp21;
    temp21= 0;
    float3 temp22;
    temp19= temp21;
    float temp23;
    temp23= 1;
    float3 temp24;
    temp20= temp23;
    temp18= lerp(temp19,temp20,round(r0.a));
    tex2.a= temp18;
    // exp.components=1 term=1
    // line 35: R0= EXPAND(T2).EXPAND(D0)
    float temp25;
    float3 temp26;
    temp26= 2 * saturate(tex2.rgb) - 1;
    float3 temp27;
    float3 temp28;
    temp28= 2 * saturate(color0.rgb) - 1;
    float3 temp29;
    float temp30;
    temp30= dot(temp26, temp28);
    temp25= temp30;
    // exp.components=1 term=1
    // line 36: R1= EXPAND(T3).EXPAND(D0)
    float temp31;
    float3 temp32;
    temp32= 2 * saturate(tex3.rgb) - 1;
    float3 temp33;
    float3 temp34;
    temp34= 2 * saturate(color0.rgb) - 1;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    // exp.components=1 term=1
    // line 37: T3a= C0a
    float temp37;
    float temp38;
    temp38= 0.752941;
    float3 temp39;
    temp37= temp38;
    r0.rgb= temp25;
    r1.rgb= temp31;
    tex3.a= temp37;
    // exp.components=1 term=1
    // line 40: T2= EXPAND(T2).EXPAND(T3)
    float temp40;
    float3 temp41;
    temp41= 2 * saturate(tex2.rgb) - 1;
    float3 temp42;
    float3 temp43;
    temp43= 2 * saturate(tex3.rgb) - 1;
    float3 temp44;
    float temp45;
    temp45= dot(temp41, temp43);
    temp40= temp45;
    // exp.components=1 term=1
    // line 41: T3= EXPAND(T2).EXPAND(D1)
    float temp46;
    float3 temp47;
    temp47= 2 * saturate(tex2.rgb) - 1;
    float3 temp48;
    float3 temp49;
    temp49= 2 * saturate(color1.rgb) - 1;
    float3 temp50;
    float temp51;
    temp51= dot(temp47, temp49);
    temp46= temp51;
    // exp.components=1 term=1
    // line 42: R0a= R0b
    float temp52;
    float temp53;
    temp53= saturate(r0.b);
    float3 temp54;
    temp52= temp53;
    tex2.rgb= temp40;
    tex3.rgb= temp46;
    r0.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: R0= T2*C0 + T3*C1
    float3 temp55;
    float3 temp56;
    float3 temp57;
    float3 temp58;
    temp58= saturate(tex2.rgb);
    float3 temp59;
    float3 temp60;
    temp60= c2.rgb;
    float3 temp61;
    float3 temp62;
    temp62= temp58 * temp60;
    temp56= temp62;
    float3 temp63;
    temp63= saturate(tex3.rgb);
    float3 temp64;
    float3 temp65;
    temp65= c3.rgb;
    float3 temp66;
    float3 temp67;
    temp67= temp63 * temp65;
    temp57= temp67;
    temp55= temp56 + temp57;
    // exp.components=1 term=1 term=1
    // line 48: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp68;
    float temp69;
    float temp70;
    float temp71;
    temp71= tex2.b;
    float3 temp72;
    float temp73;
    temp73= r0.b;
    float3 temp74;
    float temp75;
    temp75= temp71 * temp73;
    temp69= temp75;
    float temp76;
    temp76= 0.5;
    float3 temp77;
    float temp78;
    temp78= r1.b;
    float3 temp79;
    float temp80;
    temp80= temp76 * temp78;
    temp70= temp80;
    temp68= temp69 - temp70;
    temp68= 2 * temp68;
    r0.rgb= temp55;
    r1.a= temp68;
    // exp.components=3 term=3 term=3
    // line 53: D1= R0a*C0 + INVERT(R0a)*C1
    float3 temp81;
    float3 temp82;
    float3 temp83;
    float temp84;
    temp84= saturate(r0.a);
    float3 temp85;
    float3 temp86;
    temp86= c4.rgb;
    float3 temp87;
    float3 temp88;
    temp88= temp84 * temp86;
    temp82= temp88;
    float temp89;
    temp89= 1-saturate(r0.a);
    float3 temp90;
    float3 temp91;
    temp91= c5.rgb;
    float3 temp92;
    float3 temp93;
    temp93= temp89 * temp91;
    temp83= temp93;
    temp81= temp82 + temp83;
    // exp.components=1 term=1
    // line 54: D0a= R1a*R1a
    float temp94;
    float temp95;
    temp95= saturate(r1.a);
    float3 temp96;
    float temp97;
    temp97= saturate(r1.a);
    float3 temp98;
    float temp99;
    temp99= temp95 * temp97;
    temp94= temp99;
    color1.rgb= temp81;
    color0.a= temp94;
    // exp.components=1 term=1
    // line 58: R1= C0a
    float temp100;
    float temp101;
    temp101= c6.a;
    float3 temp102;
    temp100= temp101;
    // exp.components=3 term=3
    // line 60: D1= D1*C0
    float3 temp103;
    float3 temp104;
    temp104= saturate(color1.rgb);
    float3 temp105;
    float3 temp106;
    temp106= c6.rgb;
    float3 temp107;
    float3 temp108;
    temp108= temp104 * temp106;
    temp103= temp108;
    // exp.components=1 term=1
    // line 61: R1a= D0a*D0a
    float temp109;
    float temp110;
    temp110= saturate(color0.a);
    float3 temp111;
    float temp112;
    temp112= saturate(color0.a);
    float3 temp113;
    float temp114;
    temp114= temp110 * temp112;
    temp109= temp114;
    r1.rgb= temp100;
    color1.rgb= temp103;
    r1.a= temp109;
    // exp.components=3 term=3 term=3
    // line 86: D0= OUT_SCALE_BY_1/2(R0a*C0 + INVERT(R0a)*C1)
    float3 temp115;
    float3 temp116;
    float3 temp117;
    float temp118;
    temp118= saturate(r0.a);
    float3 temp119;
    float3 temp120;
    temp120= c7.rgb;
    float3 temp121;
    float3 temp122;
    temp122= temp118 * temp120;
    temp116= temp122;
    float temp123;
    temp123= 1-saturate(r0.a);
    float3 temp124;
    float3 temp125;
    temp125= c8.rgb;
    float3 temp126;
    float3 temp127;
    temp127= temp123 * temp125;
    temp117= temp127;
    temp115= temp116 + temp117;
    temp115= 0.5 * temp115;
    // exp.components=1 term=1 term=1
    // line 87: R1a= OUT_SCALE_BY_4(R1a*R1a - T3a)
    float temp128;
    float temp129;
    float temp130;
    float temp131;
    temp131= saturate(r1.a);
    float3 temp132;
    float temp133;
    temp133= saturate(r1.a);
    float3 temp134;
    float temp135;
    temp135= temp131 * temp133;
    temp129= temp135;
    float temp136;
    temp136= saturate(tex3.a);
    float3 temp137;
    temp130= temp136;
    temp128= temp129 - temp130;
    temp128= 4 * temp128;
    color0.rgb= temp115;
    r1.a= temp128;
    // exp.components=3 term=3 term=3
    // line 92: R0= OUT_SCALE_BY_2(R0*T0 + C0*T0)
    float3 temp138;
    float3 temp139;
    float3 temp140;
    float3 temp141;
    temp141= saturate(r0.rgb);
    float3 temp142;
    float3 temp143;
    temp143= saturate(tex0.rgb);
    float3 temp144;
    float3 temp145;
    temp145= temp141 * temp143;
    temp139= temp145;
    float3 temp146;
    temp146= c9.rgb;
    float3 temp147;
    float3 temp148;
    temp148= saturate(tex0.rgb);
    float3 temp149;
    float3 temp150;
    temp150= temp146 * temp148;
    temp140= temp150;
    temp138= temp139 + temp140;
    temp138= 2 * temp138;
    // exp.components=1 term=1
    // line 93: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp151;
    float temp152;
    temp152= saturate(r1.a);
    float3 temp153;
    float temp154;
    temp154= saturate(tex0.a);
    float3 temp155;
    float temp156;
    temp156= temp152 * temp154;
    temp151= temp156;
    temp151= 2 * temp151;
    // exp.components=1 term=1
    // line 94: D0a= OUT_SCALE_BY_2(R1b*T0a)
    float temp157;
    float temp158;
    temp158= saturate(r1.b);
    float3 temp159;
    float temp160;
    temp160= saturate(tex0.a);
    float3 temp161;
    float temp162;
    temp162= temp158 * temp160;
    temp157= temp162;
    temp157= 2 * temp157;
    r0.rgb= temp138;
    r1.a= temp151;
    color0.a= temp157;
    // exp.components=3 term=3
    // line 99: D0= D0*T1
    float3 temp163;
    float3 temp164;
    temp164= saturate(color0.rgb);
    float3 temp165;
    float3 temp166;
    temp166= saturate(tex1.rgb);
    float3 temp167;
    float3 temp168;
    temp168= temp164 * temp166;
    temp163= temp168;
    // exp.components=3 term=3
    // line 100: D1= D1*R1a
    float3 temp169;
    float3 temp170;
    temp170= saturate(color1.rgb);
    float3 temp171;
    float temp172;
    temp172= saturate(r1.a);
    float3 temp173;
    float3 temp174;
    temp174= temp170 * temp172;
    temp169= temp174;
    // exp.components=1 term=1 term=1
    // line 101: R0a= R0a*C0a + INVERT(R0a)*C1a
    float temp175;
    float temp176;
    float temp177;
    float temp178;
    temp178= saturate(r0.a);
    float3 temp179;
    float temp180;
    temp180= c10.a;
    float3 temp181;
    float temp182;
    temp182= temp178 * temp180;
    temp176= temp182;
    float temp183;
    temp183= 1-saturate(r0.a);
    float3 temp184;
    float temp185;
    temp185= c11.a;
    float3 temp186;
    float temp187;
    temp187= temp183 * temp185;
    temp177= temp187;
    temp175= temp176 + temp177;
    color0.rgb= temp163;
    color1.rgb= temp169;
    r0.a= temp175;
    // exp.components=3 term=3
    // line 104: E= D0
    float3 temp188;
    temp188= saturate(color0.rgb);
    float3 temp189;
    e= temp188;
    // exp.components=1 term=1
    // line 105: F= D0a
    float temp190;
    temp190= saturate(color0.a);
    float3 temp191;
    f= temp190;
    // exp.components=3 term=3 term=3
    // line 106: SRCCOLOR= EF*R0a + [R0+D1]
    float3 temp192;
    float3 temp193;
    float3 temp194;
    float3 temp195;
    float3 temp196;
    temp196= e.rgb*f.rgb;
    temp195= temp196;
    float temp197;
    temp197= saturate(r0.a);
    float3 temp198;
    float3 temp199;
    temp199= temp195 * temp197;
    temp193= temp199;
    float3 temp200;
    float3 temp201;
    temp201= r0.rgb+color1.rgb;
    temp200= temp201;
    temp194= temp200;
    temp192= temp193 + temp194;
    // exp.components=1 term=1
    // line 107: SRCALPHA= T2a
    float temp202;
    float temp203;
    temp203= saturate(tex2.a);
    float3 temp204;
    temp202= temp203;
    r0.rgb= temp192;
    r0.a= temp202;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
