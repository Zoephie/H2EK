; compiled from tags\rasterizer\pixel_shaders_dx9\shaders_shader_passes_lightmap_lightmap_tex_env_detail_alpha_test_0.txt
;
#const C1.rgb= $env_tint_color
#const C2.rgb= $env_glancing_tint_color
#const C3.a= $env_brightness
#const C4.a= $env_glancing_brightness
#const C5.a= $lightmap_specular_brightness
#const C7.rgb= $specular_color
#case $lightmap_type=0
sampler2D s0 : register(S0);
samplerCUBE s1 : register(S1);
sampler2D s2 : register(S2);
sampler2D s3 : register(S3);
sampler2D s4 : register(S4);
sampler2D s5 : register(S5);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
#ifdef CENTROID5
    float4 tc5 : TEXCOORD5_centroid,
#else
    float4 tc5 : TEXCOORD5,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 2 dot 0 input-stage 0
    float4 tex1= texCUBE(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 0 dot 0 input-stage 0
    float4 tex3= tex2Dproj(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
// texture stage 5 mode 0 dot 0 input-stage 0
    float4 tex5= tex2Dproj(s5, tc5);
    // exp.components=3 term=3
    // line 28: T0= OUT_SCALE_BY_2(T0*T4)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex4.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 29: T0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    tex0.rgb= temp0;
    tex0.a= temp6;
    // exp.components=1 term=1
    // line 31: R0a= T5a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex5.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    r0.a= temp12;
    // exp.components=1 term=1 term=1
    // line 33: T2a= 0 mux 1
    float temp18;
    float temp19;
    float temp20;
    float temp21;
    temp21= 0;
    float3 temp22;
    temp19= temp21;
    float temp23;
    temp23= 1;
    float3 temp24;
    temp20= temp23;
    temp18= lerp(temp19,temp20,round(r0.a));
    tex2.a= temp18;
    // exp.components=1 term=1
    // line 35: R0= EXPAND(T2).EXPAND(D1)
    float temp25;
    float3 temp26;
    temp26= 2 * saturate(tex2.rgb) - 1;
    float3 temp27;
    float3 temp28;
    temp28= 2 * saturate(color1.rgb) - 1;
    float3 temp29;
    float temp30;
    temp30= dot(temp26, temp28);
    temp25= temp30;
    // exp.components=1 term=1
    // line 36: T2= EXPAND(T2).EXPAND(D0)
    float temp31;
    float3 temp32;
    temp32= 2 * saturate(tex2.rgb) - 1;
    float3 temp33;
    float3 temp34;
    temp34= 2 * saturate(color0.rgb) - 1;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    // exp.components=1 term=1
    // line 37: R0a= EXPAND(D1b)
    float temp37;
    float temp38;
    temp38= 2 * saturate(color1.b) - 1;
    float3 temp39;
    temp37= temp38;
    // exp.components=1 term=1
    // line 38: T1a= EXPAND(D1a)*T0a
    float temp40;
    float temp41;
    temp41= 2 * saturate(color1.a) - 1;
    float3 temp42;
    float temp43;
    temp43= saturate(tex0.a);
    float3 temp44;
    float temp45;
    temp45= temp41 * temp43;
    temp40= temp45;
    r0.rgb= temp25;
    tex2.rgb= temp31;
    r0.a= temp37;
    tex1.a= temp40;
    // exp.components=1 term=1
    // line 41: R1= EXPAND(D1).EXPAND(D0)
    float temp46;
    float3 temp47;
    temp47= 2 * saturate(color1.rgb) - 1;
    float3 temp48;
    float3 temp49;
    temp49= 2 * saturate(color0.rgb) - 1;
    float3 temp50;
    float temp51;
    temp51= dot(temp47, temp49);
    temp46= temp51;
    // exp.components=1 term=1 term=1
    // line 42: D1a= D1a*SIGNED(R0b) - D1a*SIGNED(R0a)
    float temp52;
    float temp53;
    float temp54;
    float temp55;
    temp55= saturate(color1.a);
    float3 temp56;
    float temp57;
    temp57= r0.b;
    float3 temp58;
    float temp59;
    temp59= temp55 * temp57;
    temp53= temp59;
    float temp60;
    temp60= saturate(color1.a);
    float3 temp61;
    float temp62;
    temp62= r0.a;
    float3 temp63;
    float temp64;
    temp64= temp60 * temp62;
    temp54= temp64;
    temp52= temp53 - temp54;
    r1.rgb= temp46;
    color1.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: D0= T2*C0 + INVERT(T2)*C1
    float3 temp65;
    float3 temp66;
    float3 temp67;
    float3 temp68;
    temp68= saturate(tex2.rgb);
    float3 temp69;
    float3 temp70;
    temp70= c1.rgb;
    float3 temp71;
    float3 temp72;
    temp72= temp68 * temp70;
    temp66= temp72;
    float3 temp73;
    temp73= 1-saturate(tex2.rgb);
    float3 temp74;
    float3 temp75;
    temp75= c2.rgb;
    float3 temp76;
    float3 temp77;
    temp77= temp73 * temp75;
    temp67= temp77;
    temp65= temp66 + temp67;
    // exp.components=1 term=1 term=1
    // line 48: R1a= OUT_SCALE_BY_2(SIGNED(R0b)*SIGNED(T2b) - 1/2*SIGNED(R1b))
    float temp78;
    float temp79;
    float temp80;
    float temp81;
    temp81= r0.b;
    float3 temp82;
    float temp83;
    temp83= tex2.b;
    float3 temp84;
    float temp85;
    temp85= temp81 * temp83;
    temp79= temp85;
    float temp86;
    temp86= 0.5;
    float3 temp87;
    float temp88;
    temp88= r1.b;
    float3 temp89;
    float temp90;
    temp90= temp86 * temp88;
    temp80= temp90;
    temp78= temp79 - temp80;
    temp78= 2 * temp78;
    color0.rgb= temp65;
    r1.a= temp78;
    // exp.components=3 term=3 term=3
    // line 53: R0= T3 + T3*SIGNED(D1a)
    float3 temp91;
    float3 temp92;
    float3 temp93;
    float3 temp94;
    temp94= saturate(tex3.rgb);
    float3 temp95;
    temp92= temp94;
    float3 temp96;
    temp96= saturate(tex3.rgb);
    float3 temp97;
    float temp98;
    temp98= color1.a;
    float3 temp99;
    float3 temp100;
    temp100= temp96 * temp98;
    temp93= temp100;
    temp91= temp92 + temp93;
    // exp.components=1 term=1 term=1
    // line 54: R0a= T2b*C0a + INVERT(T2b)*C1a
    float temp101;
    float temp102;
    float temp103;
    float temp104;
    temp104= saturate(tex2.b);
    float3 temp105;
    float temp106;
    temp106= c3.a;
    float3 temp107;
    float temp108;
    temp108= temp104 * temp106;
    temp102= temp108;
    float temp109;
    temp109= 1-saturate(tex2.b);
    float3 temp110;
    float temp111;
    temp111= c4.a;
    float3 temp112;
    float temp113;
    temp113= temp109 * temp111;
    temp103= temp113;
    temp101= temp102 + temp103;
    r0.rgb= temp91;
    r0.a= temp101;
    // exp.components=1 term=1
    // line 59: R1= R0.C1a
    float temp114;
    float3 temp115;
    temp115= saturate(r0.rgb);
    float3 temp116;
    float temp117;
    temp117= 0.752941;
    float3 temp118;
    float temp119;
    temp119= dot(temp115, temp117);
    temp114= temp119;
    // exp.components=3 term=3
    // line 60: D0= D0*T1
    float3 temp120;
    float3 temp121;
    temp121= saturate(color0.rgb);
    float3 temp122;
    float3 temp123;
    temp123= saturate(tex1.rgb);
    float3 temp124;
    float3 temp125;
    temp125= temp121 * temp123;
    temp120= temp125;
    // exp.components=1 term=1
    // line 61: D1a= R1a*R1a
    float temp126;
    float temp127;
    temp127= saturate(r1.a);
    float3 temp128;
    float temp129;
    temp129= saturate(r1.a);
    float3 temp130;
    float temp131;
    temp131= temp127 * temp129;
    temp126= temp131;
    // exp.components=1 term=1
    // line 62: T1a= C0a*T1a
    float temp132;
    float temp133;
    temp133= c5.a;
    float3 temp134;
    float temp135;
    temp135= saturate(tex1.a);
    float3 temp136;
    float temp137;
    temp137= temp133 * temp135;
    temp132= temp137;
    r1.rgb= temp114;
    color0.rgb= temp120;
    color1.a= temp126;
    tex1.a= temp132;
    // exp.components=3 term=3
    // line 66: R0= OUT_SCALE_BY_2(R0*T0)
    float3 temp138;
    float3 temp139;
    temp139= saturate(r0.rgb);
    float3 temp140;
    float3 temp141;
    temp141= saturate(tex0.rgb);
    float3 temp142;
    float3 temp143;
    temp143= temp139 * temp141;
    temp138= temp143;
    temp138= 2 * temp138;
    // exp.components=3 term=3
    // line 67: T3= OUT_SCALE_BY_2(C0*T3)
    float3 temp144;
    float3 temp145;
    temp145= c7.rgb;
    float3 temp146;
    float3 temp147;
    temp147= saturate(tex3.rgb);
    float3 temp148;
    float3 temp149;
    temp149= temp145 * temp147;
    temp144= temp149;
    temp144= 2 * temp144;
    // exp.components=1 term=1
    // line 68: R1a= D1a*D1a
    float temp150;
    float temp151;
    temp151= saturate(color1.a);
    float3 temp152;
    float temp153;
    temp153= saturate(color1.a);
    float3 temp154;
    float temp155;
    temp155= temp151 * temp153;
    temp150= temp155;
    r0.rgb= temp138;
    tex3.rgb= temp144;
    r1.a= temp150;
    // exp.components=1 term=1
    // line 73: D1= 0
    float temp156;
    float temp157;
    temp157= 0;
    float3 temp158;
    temp156= temp157;
    // exp.components=1 term=1
    // line 74: R1a= 0
    float temp159;
    float temp160;
    temp160= 0;
    float3 temp161;
    temp159= temp160;
    color1.rgb= temp156;
    r1.a= temp159;
    // exp.components=3 term=3
    // line 90: D1= OUT_SCALE_BY_2(R1a*D1)
    float3 temp162;
    float temp163;
    temp163= saturate(r1.a);
    float3 temp164;
    float3 temp165;
    temp165= saturate(color1.rgb);
    float3 temp166;
    float3 temp167;
    temp167= temp163 * temp165;
    temp162= temp167;
    temp162= 2 * temp162;
    // exp.components=1 term=1
    // line 91: D0a= OUT_SCALE_BY_2(R1b*T0a)
    float temp168;
    float temp169;
    temp169= saturate(r1.b);
    float3 temp170;
    float temp171;
    temp171= saturate(tex0.a);
    float3 temp172;
    float temp173;
    temp173= temp169 * temp171;
    temp168= temp173;
    temp168= 2 * temp168;
    color1.rgb= temp162;
    color0.a= temp168;
    // exp.components=1 term=1
    // line 94: E= D0a
    float temp174;
    temp174= saturate(color0.a);
    float3 temp175;
    e= temp174;
    // exp.components=1 term=1
    // line 95: F= R0a
    float temp176;
    temp176= saturate(r0.a);
    float3 temp177;
    f= temp176;
    // exp.components=3 term=3 term=3
    // line 96: SRCCOLOR= D0*EF + [R0+D1]
    float3 temp178;
    float3 temp179;
    float3 temp180;
    float3 temp181;
    temp181= saturate(color0.rgb);
    float3 temp182;
    float3 temp183;
    float3 temp184;
    temp184= e.rgb*f.rgb;
    temp183= temp184;
    float3 temp185;
    temp185= temp181 * temp183;
    temp179= temp185;
    float3 temp186;
    float3 temp187;
    temp187= r0.rgb+color1.rgb;
    temp186= temp187;
    temp180= temp186;
    temp178= temp179 + temp180;
    // exp.components=1 term=1
    // line 97: SRCALPHA= T2a
    float temp188;
    float temp189;
    temp189= saturate(tex2.a);
    float3 temp190;
    temp188= temp189;
    r0.rgb= temp178;
    r0.a= temp188;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $lightmap_type=1
sampler2D s0 : register(S0);
samplerCUBE s1 : register(S1);
sampler2D s2 : register(S2);
sampler2D s3 : register(S3);
sampler2D s4 : register(S4);
sampler2D s5 : register(S5);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
#ifdef CENTROID5
    float4 tc5 : TEXCOORD5_centroid,
#else
    float4 tc5 : TEXCOORD5,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 2 dot 0 input-stage 0
    float4 tex1= texCUBE(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 0 dot 0 input-stage 0
    float4 tex3= tex2Dproj(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
// texture stage 5 mode 0 dot 0 input-stage 0
    float4 tex5= tex2Dproj(s5, tc5);
    // exp.components=3 term=3
    // line 28: T0= OUT_SCALE_BY_2(T0*T4)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex4.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 29: T0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    tex0.rgb= temp0;
    tex0.a= temp6;
    // exp.components=1 term=1
    // line 31: R0a= T5a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex5.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    r0.a= temp12;
    // exp.components=1 term=1 term=1
    // line 33: T2a= 0 mux 1
    float temp18;
    float temp19;
    float temp20;
    float temp21;
    temp21= 0;
    float3 temp22;
    temp19= temp21;
    float temp23;
    temp23= 1;
    float3 temp24;
    temp20= temp23;
    temp18= lerp(temp19,temp20,round(r0.a));
    tex2.a= temp18;
    // exp.components=1 term=1
    // line 35: R0= EXPAND(T2).EXPAND(D1)
    float temp25;
    float3 temp26;
    temp26= 2 * saturate(tex2.rgb) - 1;
    float3 temp27;
    float3 temp28;
    temp28= 2 * saturate(color1.rgb) - 1;
    float3 temp29;
    float temp30;
    temp30= dot(temp26, temp28);
    temp25= temp30;
    // exp.components=1 term=1
    // line 36: T2= EXPAND(T2).EXPAND(D0)
    float temp31;
    float3 temp32;
    temp32= 2 * saturate(tex2.rgb) - 1;
    float3 temp33;
    float3 temp34;
    temp34= 2 * saturate(color0.rgb) - 1;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    // exp.components=1 term=1
    // line 37: R0a= EXPAND(D1b)
    float temp37;
    float temp38;
    temp38= 2 * saturate(color1.b) - 1;
    float3 temp39;
    temp37= temp38;
    // exp.components=1 term=1
    // line 38: T1a= EXPAND(D1a)*T0a
    float temp40;
    float temp41;
    temp41= 2 * saturate(color1.a) - 1;
    float3 temp42;
    float temp43;
    temp43= saturate(tex0.a);
    float3 temp44;
    float temp45;
    temp45= temp41 * temp43;
    temp40= temp45;
    r0.rgb= temp25;
    tex2.rgb= temp31;
    r0.a= temp37;
    tex1.a= temp40;
    // exp.components=1 term=1
    // line 41: R1= EXPAND(D1).EXPAND(D0)
    float temp46;
    float3 temp47;
    temp47= 2 * saturate(color1.rgb) - 1;
    float3 temp48;
    float3 temp49;
    temp49= 2 * saturate(color0.rgb) - 1;
    float3 temp50;
    float temp51;
    temp51= dot(temp47, temp49);
    temp46= temp51;
    // exp.components=1 term=1 term=1
    // line 42: D1a= D1a*SIGNED(R0b) - D1a*SIGNED(R0a)
    float temp52;
    float temp53;
    float temp54;
    float temp55;
    temp55= saturate(color1.a);
    float3 temp56;
    float temp57;
    temp57= r0.b;
    float3 temp58;
    float temp59;
    temp59= temp55 * temp57;
    temp53= temp59;
    float temp60;
    temp60= saturate(color1.a);
    float3 temp61;
    float temp62;
    temp62= r0.a;
    float3 temp63;
    float temp64;
    temp64= temp60 * temp62;
    temp54= temp64;
    temp52= temp53 - temp54;
    r1.rgb= temp46;
    color1.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: D0= T2*C0 + INVERT(T2)*C1
    float3 temp65;
    float3 temp66;
    float3 temp67;
    float3 temp68;
    temp68= saturate(tex2.rgb);
    float3 temp69;
    float3 temp70;
    temp70= c1.rgb;
    float3 temp71;
    float3 temp72;
    temp72= temp68 * temp70;
    temp66= temp72;
    float3 temp73;
    temp73= 1-saturate(tex2.rgb);
    float3 temp74;
    float3 temp75;
    temp75= c2.rgb;
    float3 temp76;
    float3 temp77;
    temp77= temp73 * temp75;
    temp67= temp77;
    temp65= temp66 + temp67;
    // exp.components=1 term=1 term=1
    // line 48: R1a= OUT_SCALE_BY_2(SIGNED(R0b)*SIGNED(T2b) - 1/2*SIGNED(R1b))
    float temp78;
    float temp79;
    float temp80;
    float temp81;
    temp81= r0.b;
    float3 temp82;
    float temp83;
    temp83= tex2.b;
    float3 temp84;
    float temp85;
    temp85= temp81 * temp83;
    temp79= temp85;
    float temp86;
    temp86= 0.5;
    float3 temp87;
    float temp88;
    temp88= r1.b;
    float3 temp89;
    float temp90;
    temp90= temp86 * temp88;
    temp80= temp90;
    temp78= temp79 - temp80;
    temp78= 2 * temp78;
    color0.rgb= temp65;
    r1.a= temp78;
    // exp.components=3 term=3 term=3
    // line 53: R0= T3 + T3*SIGNED(D1a)
    float3 temp91;
    float3 temp92;
    float3 temp93;
    float3 temp94;
    temp94= saturate(tex3.rgb);
    float3 temp95;
    temp92= temp94;
    float3 temp96;
    temp96= saturate(tex3.rgb);
    float3 temp97;
    float temp98;
    temp98= color1.a;
    float3 temp99;
    float3 temp100;
    temp100= temp96 * temp98;
    temp93= temp100;
    temp91= temp92 + temp93;
    // exp.components=1 term=1 term=1
    // line 54: R0a= T2b*C0a + INVERT(T2b)*C1a
    float temp101;
    float temp102;
    float temp103;
    float temp104;
    temp104= saturate(tex2.b);
    float3 temp105;
    float temp106;
    temp106= c3.a;
    float3 temp107;
    float temp108;
    temp108= temp104 * temp106;
    temp102= temp108;
    float temp109;
    temp109= 1-saturate(tex2.b);
    float3 temp110;
    float temp111;
    temp111= c4.a;
    float3 temp112;
    float temp113;
    temp113= temp109 * temp111;
    temp103= temp113;
    temp101= temp102 + temp103;
    r0.rgb= temp91;
    r0.a= temp101;
    // exp.components=1 term=1
    // line 59: R1= R0.C1a
    float temp114;
    float3 temp115;
    temp115= saturate(r0.rgb);
    float3 temp116;
    float temp117;
    temp117= 0.752941;
    float3 temp118;
    float temp119;
    temp119= dot(temp115, temp117);
    temp114= temp119;
    // exp.components=3 term=3
    // line 60: D0= D0*T1
    float3 temp120;
    float3 temp121;
    temp121= saturate(color0.rgb);
    float3 temp122;
    float3 temp123;
    temp123= saturate(tex1.rgb);
    float3 temp124;
    float3 temp125;
    temp125= temp121 * temp123;
    temp120= temp125;
    // exp.components=1 term=1
    // line 61: D1a= R1a*R1a
    float temp126;
    float temp127;
    temp127= saturate(r1.a);
    float3 temp128;
    float temp129;
    temp129= saturate(r1.a);
    float3 temp130;
    float temp131;
    temp131= temp127 * temp129;
    temp126= temp131;
    // exp.components=1 term=1
    // line 62: T1a= C0a*T1a
    float temp132;
    float temp133;
    temp133= c5.a;
    float3 temp134;
    float temp135;
    temp135= saturate(tex1.a);
    float3 temp136;
    float temp137;
    temp137= temp133 * temp135;
    temp132= temp137;
    r1.rgb= temp114;
    color0.rgb= temp120;
    color1.a= temp126;
    tex1.a= temp132;
    // exp.components=3 term=3
    // line 66: R0= OUT_SCALE_BY_2(R0*T0)
    float3 temp138;
    float3 temp139;
    temp139= saturate(r0.rgb);
    float3 temp140;
    float3 temp141;
    temp141= saturate(tex0.rgb);
    float3 temp142;
    float3 temp143;
    temp143= temp139 * temp141;
    temp138= temp143;
    temp138= 2 * temp138;
    // exp.components=3 term=3
    // line 67: T3= OUT_SCALE_BY_2(C0*T3)
    float3 temp144;
    float3 temp145;
    temp145= c7.rgb;
    float3 temp146;
    float3 temp147;
    temp147= saturate(tex3.rgb);
    float3 temp148;
    float3 temp149;
    temp149= temp145 * temp147;
    temp144= temp149;
    temp144= 2 * temp144;
    // exp.components=1 term=1
    // line 68: R1a= D1a*D1a
    float temp150;
    float temp151;
    temp151= saturate(color1.a);
    float3 temp152;
    float temp153;
    temp153= saturate(color1.a);
    float3 temp154;
    float temp155;
    temp155= temp151 * temp153;
    temp150= temp155;
    r0.rgb= temp138;
    tex3.rgb= temp144;
    r1.a= temp150;
    // exp.components=3 term=3
    // line 77: D1= OUT_SCALE_BY_2(T3*T1a)
    float3 temp156;
    float3 temp157;
    temp157= saturate(tex3.rgb);
    float3 temp158;
    float temp159;
    temp159= saturate(tex1.a);
    float3 temp160;
    float3 temp161;
    temp161= temp157 * temp159;
    temp156= temp161;
    temp156= 2 * temp156;
    // exp.components=1 term=1
    // line 78: R1a= R1a*R1a
    float temp162;
    float temp163;
    temp163= saturate(r1.a);
    float3 temp164;
    float temp165;
    temp165= saturate(r1.a);
    float3 temp166;
    float temp167;
    temp167= temp163 * temp165;
    temp162= temp167;
    color1.rgb= temp156;
    r1.a= temp162;
    // exp.components=3 term=3
    // line 90: D1= OUT_SCALE_BY_2(R1a*D1)
    float3 temp168;
    float temp169;
    temp169= saturate(r1.a);
    float3 temp170;
    float3 temp171;
    temp171= saturate(color1.rgb);
    float3 temp172;
    float3 temp173;
    temp173= temp169 * temp171;
    temp168= temp173;
    temp168= 2 * temp168;
    // exp.components=1 term=1
    // line 91: D0a= OUT_SCALE_BY_2(R1b*T0a)
    float temp174;
    float temp175;
    temp175= saturate(r1.b);
    float3 temp176;
    float temp177;
    temp177= saturate(tex0.a);
    float3 temp178;
    float temp179;
    temp179= temp175 * temp177;
    temp174= temp179;
    temp174= 2 * temp174;
    color1.rgb= temp168;
    color0.a= temp174;
    // exp.components=1 term=1
    // line 94: E= D0a
    float temp180;
    temp180= saturate(color0.a);
    float3 temp181;
    e= temp180;
    // exp.components=1 term=1
    // line 95: F= R0a
    float temp182;
    temp182= saturate(r0.a);
    float3 temp183;
    f= temp182;
    // exp.components=3 term=3 term=3
    // line 96: SRCCOLOR= D0*EF + [R0+D1]
    float3 temp184;
    float3 temp185;
    float3 temp186;
    float3 temp187;
    temp187= saturate(color0.rgb);
    float3 temp188;
    float3 temp189;
    float3 temp190;
    temp190= e.rgb*f.rgb;
    temp189= temp190;
    float3 temp191;
    temp191= temp187 * temp189;
    temp185= temp191;
    float3 temp192;
    float3 temp193;
    temp193= r0.rgb+color1.rgb;
    temp192= temp193;
    temp186= temp192;
    temp184= temp185 + temp186;
    // exp.components=1 term=1
    // line 97: SRCALPHA= T2a
    float temp194;
    float temp195;
    temp195= saturate(tex2.a);
    float3 temp196;
    temp194= temp195;
    r0.rgb= temp184;
    r0.a= temp194;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $lightmap_type=2
sampler2D s0 : register(S0);
samplerCUBE s1 : register(S1);
sampler2D s2 : register(S2);
sampler2D s3 : register(S3);
sampler2D s4 : register(S4);
sampler2D s5 : register(S5);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
#ifdef CENTROID5
    float4 tc5 : TEXCOORD5_centroid,
#else
    float4 tc5 : TEXCOORD5,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 2 dot 0 input-stage 0
    float4 tex1= texCUBE(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 0 dot 0 input-stage 0
    float4 tex3= tex2Dproj(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
// texture stage 5 mode 0 dot 0 input-stage 0
    float4 tex5= tex2Dproj(s5, tc5);
    // exp.components=3 term=3
    // line 28: T0= OUT_SCALE_BY_2(T0*T4)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex4.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 29: T0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    tex0.rgb= temp0;
    tex0.a= temp6;
    // exp.components=1 term=1
    // line 31: R0a= T5a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex5.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    r0.a= temp12;
    // exp.components=1 term=1 term=1
    // line 33: T2a= 0 mux 1
    float temp18;
    float temp19;
    float temp20;
    float temp21;
    temp21= 0;
    float3 temp22;
    temp19= temp21;
    float temp23;
    temp23= 1;
    float3 temp24;
    temp20= temp23;
    temp18= lerp(temp19,temp20,round(r0.a));
    tex2.a= temp18;
    // exp.components=1 term=1
    // line 35: R0= EXPAND(T2).EXPAND(D1)
    float temp25;
    float3 temp26;
    temp26= 2 * saturate(tex2.rgb) - 1;
    float3 temp27;
    float3 temp28;
    temp28= 2 * saturate(color1.rgb) - 1;
    float3 temp29;
    float temp30;
    temp30= dot(temp26, temp28);
    temp25= temp30;
    // exp.components=1 term=1
    // line 36: T2= EXPAND(T2).EXPAND(D0)
    float temp31;
    float3 temp32;
    temp32= 2 * saturate(tex2.rgb) - 1;
    float3 temp33;
    float3 temp34;
    temp34= 2 * saturate(color0.rgb) - 1;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    // exp.components=1 term=1
    // line 37: R0a= EXPAND(D1b)
    float temp37;
    float temp38;
    temp38= 2 * saturate(color1.b) - 1;
    float3 temp39;
    temp37= temp38;
    // exp.components=1 term=1
    // line 38: T1a= EXPAND(D1a)*T0a
    float temp40;
    float temp41;
    temp41= 2 * saturate(color1.a) - 1;
    float3 temp42;
    float temp43;
    temp43= saturate(tex0.a);
    float3 temp44;
    float temp45;
    temp45= temp41 * temp43;
    temp40= temp45;
    r0.rgb= temp25;
    tex2.rgb= temp31;
    r0.a= temp37;
    tex1.a= temp40;
    // exp.components=1 term=1
    // line 41: R1= EXPAND(D1).EXPAND(D0)
    float temp46;
    float3 temp47;
    temp47= 2 * saturate(color1.rgb) - 1;
    float3 temp48;
    float3 temp49;
    temp49= 2 * saturate(color0.rgb) - 1;
    float3 temp50;
    float temp51;
    temp51= dot(temp47, temp49);
    temp46= temp51;
    // exp.components=1 term=1 term=1
    // line 42: D1a= D1a*SIGNED(R0b) - D1a*SIGNED(R0a)
    float temp52;
    float temp53;
    float temp54;
    float temp55;
    temp55= saturate(color1.a);
    float3 temp56;
    float temp57;
    temp57= r0.b;
    float3 temp58;
    float temp59;
    temp59= temp55 * temp57;
    temp53= temp59;
    float temp60;
    temp60= saturate(color1.a);
    float3 temp61;
    float temp62;
    temp62= r0.a;
    float3 temp63;
    float temp64;
    temp64= temp60 * temp62;
    temp54= temp64;
    temp52= temp53 - temp54;
    r1.rgb= temp46;
    color1.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: D0= T2*C0 + INVERT(T2)*C1
    float3 temp65;
    float3 temp66;
    float3 temp67;
    float3 temp68;
    temp68= saturate(tex2.rgb);
    float3 temp69;
    float3 temp70;
    temp70= c1.rgb;
    float3 temp71;
    float3 temp72;
    temp72= temp68 * temp70;
    temp66= temp72;
    float3 temp73;
    temp73= 1-saturate(tex2.rgb);
    float3 temp74;
    float3 temp75;
    temp75= c2.rgb;
    float3 temp76;
    float3 temp77;
    temp77= temp73 * temp75;
    temp67= temp77;
    temp65= temp66 + temp67;
    // exp.components=1 term=1 term=1
    // line 48: R1a= OUT_SCALE_BY_2(SIGNED(R0b)*SIGNED(T2b) - 1/2*SIGNED(R1b))
    float temp78;
    float temp79;
    float temp80;
    float temp81;
    temp81= r0.b;
    float3 temp82;
    float temp83;
    temp83= tex2.b;
    float3 temp84;
    float temp85;
    temp85= temp81 * temp83;
    temp79= temp85;
    float temp86;
    temp86= 0.5;
    float3 temp87;
    float temp88;
    temp88= r1.b;
    float3 temp89;
    float temp90;
    temp90= temp86 * temp88;
    temp80= temp90;
    temp78= temp79 - temp80;
    temp78= 2 * temp78;
    color0.rgb= temp65;
    r1.a= temp78;
    // exp.components=3 term=3 term=3
    // line 53: R0= T3 + T3*SIGNED(D1a)
    float3 temp91;
    float3 temp92;
    float3 temp93;
    float3 temp94;
    temp94= saturate(tex3.rgb);
    float3 temp95;
    temp92= temp94;
    float3 temp96;
    temp96= saturate(tex3.rgb);
    float3 temp97;
    float temp98;
    temp98= color1.a;
    float3 temp99;
    float3 temp100;
    temp100= temp96 * temp98;
    temp93= temp100;
    temp91= temp92 + temp93;
    // exp.components=1 term=1 term=1
    // line 54: R0a= T2b*C0a + INVERT(T2b)*C1a
    float temp101;
    float temp102;
    float temp103;
    float temp104;
    temp104= saturate(tex2.b);
    float3 temp105;
    float temp106;
    temp106= c3.a;
    float3 temp107;
    float temp108;
    temp108= temp104 * temp106;
    temp102= temp108;
    float temp109;
    temp109= 1-saturate(tex2.b);
    float3 temp110;
    float temp111;
    temp111= c4.a;
    float3 temp112;
    float temp113;
    temp113= temp109 * temp111;
    temp103= temp113;
    temp101= temp102 + temp103;
    r0.rgb= temp91;
    r0.a= temp101;
    // exp.components=1 term=1
    // line 59: R1= R0.C1a
    float temp114;
    float3 temp115;
    temp115= saturate(r0.rgb);
    float3 temp116;
    float temp117;
    temp117= 0.752941;
    float3 temp118;
    float temp119;
    temp119= dot(temp115, temp117);
    temp114= temp119;
    // exp.components=3 term=3
    // line 60: D0= D0*T1
    float3 temp120;
    float3 temp121;
    temp121= saturate(color0.rgb);
    float3 temp122;
    float3 temp123;
    temp123= saturate(tex1.rgb);
    float3 temp124;
    float3 temp125;
    temp125= temp121 * temp123;
    temp120= temp125;
    // exp.components=1 term=1
    // line 61: D1a= R1a*R1a
    float temp126;
    float temp127;
    temp127= saturate(r1.a);
    float3 temp128;
    float temp129;
    temp129= saturate(r1.a);
    float3 temp130;
    float temp131;
    temp131= temp127 * temp129;
    temp126= temp131;
    // exp.components=1 term=1
    // line 62: T1a= C0a*T1a
    float temp132;
    float temp133;
    temp133= c5.a;
    float3 temp134;
    float temp135;
    temp135= saturate(tex1.a);
    float3 temp136;
    float temp137;
    temp137= temp133 * temp135;
    temp132= temp137;
    r1.rgb= temp114;
    color0.rgb= temp120;
    color1.a= temp126;
    tex1.a= temp132;
    // exp.components=3 term=3
    // line 66: R0= OUT_SCALE_BY_2(R0*T0)
    float3 temp138;
    float3 temp139;
    temp139= saturate(r0.rgb);
    float3 temp140;
    float3 temp141;
    temp141= saturate(tex0.rgb);
    float3 temp142;
    float3 temp143;
    temp143= temp139 * temp141;
    temp138= temp143;
    temp138= 2 * temp138;
    // exp.components=3 term=3
    // line 67: T3= OUT_SCALE_BY_2(C0*T3)
    float3 temp144;
    float3 temp145;
    temp145= c7.rgb;
    float3 temp146;
    float3 temp147;
    temp147= saturate(tex3.rgb);
    float3 temp148;
    float3 temp149;
    temp149= temp145 * temp147;
    temp144= temp149;
    temp144= 2 * temp144;
    // exp.components=1 term=1
    // line 68: R1a= D1a*D1a
    float temp150;
    float temp151;
    temp151= saturate(color1.a);
    float3 temp152;
    float temp153;
    temp153= saturate(color1.a);
    float3 temp154;
    float temp155;
    temp155= temp151 * temp153;
    temp150= temp155;
    r0.rgb= temp138;
    tex3.rgb= temp144;
    r1.a= temp150;
    // exp.components=3 term=3
    // line 81: D1= OUT_SCALE_BY_2(T3*T1a)
    float3 temp156;
    float3 temp157;
    temp157= saturate(tex3.rgb);
    float3 temp158;
    float temp159;
    temp159= saturate(tex1.a);
    float3 temp160;
    float3 temp161;
    temp161= temp157 * temp159;
    temp156= temp161;
    temp156= 2 * temp156;
    // exp.components=1 term=1
    // line 82: R1a= D1a
    float temp162;
    float temp163;
    temp163= saturate(color1.a);
    float3 temp164;
    temp162= temp163;
    color1.rgb= temp156;
    r1.a= temp162;
    // exp.components=3 term=3
    // line 90: D1= OUT_SCALE_BY_2(R1a*D1)
    float3 temp165;
    float temp166;
    temp166= saturate(r1.a);
    float3 temp167;
    float3 temp168;
    temp168= saturate(color1.rgb);
    float3 temp169;
    float3 temp170;
    temp170= temp166 * temp168;
    temp165= temp170;
    temp165= 2 * temp165;
    // exp.components=1 term=1
    // line 91: D0a= OUT_SCALE_BY_2(R1b*T0a)
    float temp171;
    float temp172;
    temp172= saturate(r1.b);
    float3 temp173;
    float temp174;
    temp174= saturate(tex0.a);
    float3 temp175;
    float temp176;
    temp176= temp172 * temp174;
    temp171= temp176;
    temp171= 2 * temp171;
    color1.rgb= temp165;
    color0.a= temp171;
    // exp.components=1 term=1
    // line 94: E= D0a
    float temp177;
    temp177= saturate(color0.a);
    float3 temp178;
    e= temp177;
    // exp.components=1 term=1
    // line 95: F= R0a
    float temp179;
    temp179= saturate(r0.a);
    float3 temp180;
    f= temp179;
    // exp.components=3 term=3 term=3
    // line 96: SRCCOLOR= D0*EF + [R0+D1]
    float3 temp181;
    float3 temp182;
    float3 temp183;
    float3 temp184;
    temp184= saturate(color0.rgb);
    float3 temp185;
    float3 temp186;
    float3 temp187;
    temp187= e.rgb*f.rgb;
    temp186= temp187;
    float3 temp188;
    temp188= temp184 * temp186;
    temp182= temp188;
    float3 temp189;
    float3 temp190;
    temp190= r0.rgb+color1.rgb;
    temp189= temp190;
    temp183= temp189;
    temp181= temp182 + temp183;
    // exp.components=1 term=1
    // line 97: SRCALPHA= T2a
    float temp191;
    float temp192;
    temp192= saturate(tex2.a);
    float3 temp193;
    temp191= temp192;
    r0.rgb= temp181;
    r0.a= temp191;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $lightmap_type=3
sampler2D s0 : register(S0);
samplerCUBE s1 : register(S1);
sampler2D s2 : register(S2);
sampler2D s3 : register(S3);
sampler2D s4 : register(S4);
sampler2D s5 : register(S5);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
#ifdef CENTROID5
    float4 tc5 : TEXCOORD5_centroid,
#else
    float4 tc5 : TEXCOORD5,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 2 dot 0 input-stage 0
    float4 tex1= texCUBE(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 0 dot 0 input-stage 0
    float4 tex3= tex2Dproj(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
// texture stage 5 mode 0 dot 0 input-stage 0
    float4 tex5= tex2Dproj(s5, tc5);
    // exp.components=3 term=3
    // line 28: T0= OUT_SCALE_BY_2(T0*T4)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex4.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 29: T0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    tex0.rgb= temp0;
    tex0.a= temp6;
    // exp.components=1 term=1
    // line 31: R0a= T5a*T2a
    float temp12;
    float temp13;
    temp13= saturate(tex5.a);
    float3 temp14;
    float temp15;
    temp15= saturate(tex2.a);
    float3 temp16;
    float temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    r0.a= temp12;
    // exp.components=1 term=1 term=1
    // line 33: T2a= 0 mux 1
    float temp18;
    float temp19;
    float temp20;
    float temp21;
    temp21= 0;
    float3 temp22;
    temp19= temp21;
    float temp23;
    temp23= 1;
    float3 temp24;
    temp20= temp23;
    temp18= lerp(temp19,temp20,round(r0.a));
    tex2.a= temp18;
    // exp.components=1 term=1
    // line 35: R0= EXPAND(T2).EXPAND(D1)
    float temp25;
    float3 temp26;
    temp26= 2 * saturate(tex2.rgb) - 1;
    float3 temp27;
    float3 temp28;
    temp28= 2 * saturate(color1.rgb) - 1;
    float3 temp29;
    float temp30;
    temp30= dot(temp26, temp28);
    temp25= temp30;
    // exp.components=1 term=1
    // line 36: T2= EXPAND(T2).EXPAND(D0)
    float temp31;
    float3 temp32;
    temp32= 2 * saturate(tex2.rgb) - 1;
    float3 temp33;
    float3 temp34;
    temp34= 2 * saturate(color0.rgb) - 1;
    float3 temp35;
    float temp36;
    temp36= dot(temp32, temp34);
    temp31= temp36;
    // exp.components=1 term=1
    // line 37: R0a= EXPAND(D1b)
    float temp37;
    float temp38;
    temp38= 2 * saturate(color1.b) - 1;
    float3 temp39;
    temp37= temp38;
    // exp.components=1 term=1
    // line 38: T1a= EXPAND(D1a)*T0a
    float temp40;
    float temp41;
    temp41= 2 * saturate(color1.a) - 1;
    float3 temp42;
    float temp43;
    temp43= saturate(tex0.a);
    float3 temp44;
    float temp45;
    temp45= temp41 * temp43;
    temp40= temp45;
    r0.rgb= temp25;
    tex2.rgb= temp31;
    r0.a= temp37;
    tex1.a= temp40;
    // exp.components=1 term=1
    // line 41: R1= EXPAND(D1).EXPAND(D0)
    float temp46;
    float3 temp47;
    temp47= 2 * saturate(color1.rgb) - 1;
    float3 temp48;
    float3 temp49;
    temp49= 2 * saturate(color0.rgb) - 1;
    float3 temp50;
    float temp51;
    temp51= dot(temp47, temp49);
    temp46= temp51;
    // exp.components=1 term=1 term=1
    // line 42: D1a= D1a*SIGNED(R0b) - D1a*SIGNED(R0a)
    float temp52;
    float temp53;
    float temp54;
    float temp55;
    temp55= saturate(color1.a);
    float3 temp56;
    float temp57;
    temp57= r0.b;
    float3 temp58;
    float temp59;
    temp59= temp55 * temp57;
    temp53= temp59;
    float temp60;
    temp60= saturate(color1.a);
    float3 temp61;
    float temp62;
    temp62= r0.a;
    float3 temp63;
    float temp64;
    temp64= temp60 * temp62;
    temp54= temp64;
    temp52= temp53 - temp54;
    r1.rgb= temp46;
    color1.a= temp52;
    // exp.components=3 term=3 term=3
    // line 47: D0= T2*C0 + INVERT(T2)*C1
    float3 temp65;
    float3 temp66;
    float3 temp67;
    float3 temp68;
    temp68= saturate(tex2.rgb);
    float3 temp69;
    float3 temp70;
    temp70= c1.rgb;
    float3 temp71;
    float3 temp72;
    temp72= temp68 * temp70;
    temp66= temp72;
    float3 temp73;
    temp73= 1-saturate(tex2.rgb);
    float3 temp74;
    float3 temp75;
    temp75= c2.rgb;
    float3 temp76;
    float3 temp77;
    temp77= temp73 * temp75;
    temp67= temp77;
    temp65= temp66 + temp67;
    // exp.components=1 term=1 term=1
    // line 48: R1a= OUT_SCALE_BY_2(SIGNED(R0b)*SIGNED(T2b) - 1/2*SIGNED(R1b))
    float temp78;
    float temp79;
    float temp80;
    float temp81;
    temp81= r0.b;
    float3 temp82;
    float temp83;
    temp83= tex2.b;
    float3 temp84;
    float temp85;
    temp85= temp81 * temp83;
    temp79= temp85;
    float temp86;
    temp86= 0.5;
    float3 temp87;
    float temp88;
    temp88= r1.b;
    float3 temp89;
    float temp90;
    temp90= temp86 * temp88;
    temp80= temp90;
    temp78= temp79 - temp80;
    temp78= 2 * temp78;
    color0.rgb= temp65;
    r1.a= temp78;
    // exp.components=3 term=3 term=3
    // line 53: R0= T3 + T3*SIGNED(D1a)
    float3 temp91;
    float3 temp92;
    float3 temp93;
    float3 temp94;
    temp94= saturate(tex3.rgb);
    float3 temp95;
    temp92= temp94;
    float3 temp96;
    temp96= saturate(tex3.rgb);
    float3 temp97;
    float temp98;
    temp98= color1.a;
    float3 temp99;
    float3 temp100;
    temp100= temp96 * temp98;
    temp93= temp100;
    temp91= temp92 + temp93;
    // exp.components=1 term=1 term=1
    // line 54: R0a= T2b*C0a + INVERT(T2b)*C1a
    float temp101;
    float temp102;
    float temp103;
    float temp104;
    temp104= saturate(tex2.b);
    float3 temp105;
    float temp106;
    temp106= c3.a;
    float3 temp107;
    float temp108;
    temp108= temp104 * temp106;
    temp102= temp108;
    float temp109;
    temp109= 1-saturate(tex2.b);
    float3 temp110;
    float temp111;
    temp111= c4.a;
    float3 temp112;
    float temp113;
    temp113= temp109 * temp111;
    temp103= temp113;
    temp101= temp102 + temp103;
    r0.rgb= temp91;
    r0.a= temp101;
    // exp.components=1 term=1
    // line 59: R1= R0.C1a
    float temp114;
    float3 temp115;
    temp115= saturate(r0.rgb);
    float3 temp116;
    float temp117;
    temp117= 0.752941;
    float3 temp118;
    float temp119;
    temp119= dot(temp115, temp117);
    temp114= temp119;
    // exp.components=3 term=3
    // line 60: D0= D0*T1
    float3 temp120;
    float3 temp121;
    temp121= saturate(color0.rgb);
    float3 temp122;
    float3 temp123;
    temp123= saturate(tex1.rgb);
    float3 temp124;
    float3 temp125;
    temp125= temp121 * temp123;
    temp120= temp125;
    // exp.components=1 term=1
    // line 61: D1a= R1a*R1a
    float temp126;
    float temp127;
    temp127= saturate(r1.a);
    float3 temp128;
    float temp129;
    temp129= saturate(r1.a);
    float3 temp130;
    float temp131;
    temp131= temp127 * temp129;
    temp126= temp131;
    // exp.components=1 term=1
    // line 62: T1a= C0a*T1a
    float temp132;
    float temp133;
    temp133= c5.a;
    float3 temp134;
    float temp135;
    temp135= saturate(tex1.a);
    float3 temp136;
    float temp137;
    temp137= temp133 * temp135;
    temp132= temp137;
    r1.rgb= temp114;
    color0.rgb= temp120;
    color1.a= temp126;
    tex1.a= temp132;
    // exp.components=3 term=3
    // line 66: R0= OUT_SCALE_BY_2(R0*T0)
    float3 temp138;
    float3 temp139;
    temp139= saturate(r0.rgb);
    float3 temp140;
    float3 temp141;
    temp141= saturate(tex0.rgb);
    float3 temp142;
    float3 temp143;
    temp143= temp139 * temp141;
    temp138= temp143;
    temp138= 2 * temp138;
    // exp.components=3 term=3
    // line 67: T3= OUT_SCALE_BY_2(C0*T3)
    float3 temp144;
    float3 temp145;
    temp145= c7.rgb;
    float3 temp146;
    float3 temp147;
    temp147= saturate(tex3.rgb);
    float3 temp148;
    float3 temp149;
    temp149= temp145 * temp147;
    temp144= temp149;
    temp144= 2 * temp144;
    // exp.components=1 term=1
    // line 68: R1a= D1a*D1a
    float temp150;
    float temp151;
    temp151= saturate(color1.a);
    float3 temp152;
    float temp153;
    temp153= saturate(color1.a);
    float3 temp154;
    float temp155;
    temp155= temp151 * temp153;
    temp150= temp155;
    r0.rgb= temp138;
    tex3.rgb= temp144;
    r1.a= temp150;
    // exp.components=3 term=3
    // line 85: D1= OUT_SCALE_BY_2(T3*T1a)
    float3 temp156;
    float3 temp157;
    temp157= saturate(tex3.rgb);
    float3 temp158;
    float temp159;
    temp159= saturate(tex1.a);
    float3 temp160;
    float3 temp161;
    temp161= temp157 * temp159;
    temp156= temp161;
    temp156= 2 * temp156;
    // exp.components=1 term=1 term=1
    // line 86: R1a= OUT_SCALE_BY_2(R1a*R1a - 1/2)
    float temp162;
    float temp163;
    float temp164;
    float temp165;
    temp165= saturate(r1.a);
    float3 temp166;
    float temp167;
    temp167= saturate(r1.a);
    float3 temp168;
    float temp169;
    temp169= temp165 * temp167;
    temp163= temp169;
    float temp170;
    temp170= 0.5;
    float3 temp171;
    temp164= temp170;
    temp162= temp163 - temp164;
    temp162= 2 * temp162;
    color1.rgb= temp156;
    r1.a= temp162;
    // exp.components=3 term=3
    // line 90: D1= OUT_SCALE_BY_2(R1a*D1)
    float3 temp172;
    float temp173;
    temp173= saturate(r1.a);
    float3 temp174;
    float3 temp175;
    temp175= saturate(color1.rgb);
    float3 temp176;
    float3 temp177;
    temp177= temp173 * temp175;
    temp172= temp177;
    temp172= 2 * temp172;
    // exp.components=1 term=1
    // line 91: D0a= OUT_SCALE_BY_2(R1b*T0a)
    float temp178;
    float temp179;
    temp179= saturate(r1.b);
    float3 temp180;
    float temp181;
    temp181= saturate(tex0.a);
    float3 temp182;
    float temp183;
    temp183= temp179 * temp181;
    temp178= temp183;
    temp178= 2 * temp178;
    color1.rgb= temp172;
    color0.a= temp178;
    // exp.components=1 term=1
    // line 94: E= D0a
    float temp184;
    temp184= saturate(color0.a);
    float3 temp185;
    e= temp184;
    // exp.components=1 term=1
    // line 95: F= R0a
    float temp186;
    temp186= saturate(r0.a);
    float3 temp187;
    f= temp186;
    // exp.components=3 term=3 term=3
    // line 96: SRCCOLOR= D0*EF + [R0+D1]
    float3 temp188;
    float3 temp189;
    float3 temp190;
    float3 temp191;
    temp191= saturate(color0.rgb);
    float3 temp192;
    float3 temp193;
    float3 temp194;
    temp194= e.rgb*f.rgb;
    temp193= temp194;
    float3 temp195;
    temp195= temp191 * temp193;
    temp189= temp195;
    float3 temp196;
    float3 temp197;
    temp197= r0.rgb+color1.rgb;
    temp196= temp197;
    temp190= temp196;
    temp188= temp189 + temp190;
    // exp.components=1 term=1
    // line 97: SRCALPHA= T2a
    float temp198;
    float temp199;
    temp199= saturate(tex2.a);
    float3 temp200;
    temp198= temp199;
    r0.rgb= temp188;
    r0.a= temp198;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
