; compiled from tags\rasterizer\pixel_shaders_dx9\shaders_shader_passes_lightmap_lightmap_tex_detail_alpha_test_detail_2.txt
;
#const C2.rgb= $lightmap_direct_color
#const C3.rgb= $lightmap_indirect_color
#const C4.rgb= $specular_color
#const C5.rgb= $specular_glancing_color
#const C6.rgb= $lightmap_specular_color_times_one_half
#const C7.rgb= $lightmap_ambient_color
#case $specular_type=0
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=3 term=3
    // line 24: R0= OUT_SCALE_BY_2(T0*T1)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex1.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 25: R0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    r0.rgb= temp0;
    r0.a= temp6;
    // exp.components=3 term=3
    // line 27: T0= OUT_SCALE_BY_2(R0*T4)
    float3 temp12;
    float3 temp13;
    temp13= saturate(r0.rgb);
    float3 temp14;
    float3 temp15;
    temp15= saturate(tex4.rgb);
    float3 temp16;
    float3 temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    temp12= 2 * temp12;
    // exp.components=1 term=1
    // line 28: R0a= R0a*T1a
    float temp18;
    float temp19;
    temp19= saturate(r0.a);
    float3 temp20;
    float temp21;
    temp21= saturate(tex1.a);
    float3 temp22;
    float temp23;
    temp23= temp19 * temp21;
    temp18= temp23;
    tex0.rgb= temp12;
    r0.a= temp18;
    // exp.components=1 term=1
    // line 31: R0= EXPAND(T2).EXPAND(D0)
    float temp24;
    float3 temp25;
    temp25= 2 * saturate(tex2.rgb) - 1;
    float3 temp26;
    float3 temp27;
    temp27= 2 * saturate(color0.rgb) - 1;
    float3 temp28;
    float temp29;
    temp29= dot(temp25, temp27);
    temp24= temp29;
    // exp.components=1 term=1
    // line 32: R1= EXPAND(T3).EXPAND(D0)
    float temp30;
    float3 temp31;
    temp31= 2 * saturate(tex3.rgb) - 1;
    float3 temp32;
    float3 temp33;
    temp33= 2 * saturate(color0.rgb) - 1;
    float3 temp34;
    float temp35;
    temp35= dot(temp31, temp33);
    temp30= temp35;
    // exp.components=1 term=1
    // line 33: T3a= C0a
    float temp36;
    float temp37;
    temp37= 0.752941;
    float3 temp38;
    temp36= temp37;
    r0.rgb= temp24;
    r1.rgb= temp30;
    tex3.a= temp36;
    // exp.components=1 term=1
    // line 36: T2= EXPAND(T2).EXPAND(T3)
    float temp39;
    float3 temp40;
    temp40= 2 * saturate(tex2.rgb) - 1;
    float3 temp41;
    float3 temp42;
    temp42= 2 * saturate(tex3.rgb) - 1;
    float3 temp43;
    float temp44;
    temp44= dot(temp40, temp42);
    temp39= temp44;
    // exp.components=1 term=1
    // line 37: T3= EXPAND(T2).EXPAND(D1)
    float temp45;
    float3 temp46;
    temp46= 2 * saturate(tex2.rgb) - 1;
    float3 temp47;
    float3 temp48;
    temp48= 2 * saturate(color1.rgb) - 1;
    float3 temp49;
    float temp50;
    temp50= dot(temp46, temp48);
    temp45= temp50;
    // exp.components=1 term=1
    // line 38: D0a= R0b
    float temp51;
    float temp52;
    temp52= saturate(r0.b);
    float3 temp53;
    temp51= temp52;
    // exp.components=1 term=1
    // line 39: T2a= EXPAND(T2b)
    float temp54;
    float temp55;
    temp55= 2 * saturate(tex2.b) - 1;
    float3 temp56;
    temp54= temp55;
    tex2.rgb= temp39;
    tex3.rgb= temp45;
    color0.a= temp51;
    tex2.a= temp54;
    // exp.components=3 term=3 term=3
    // line 44: R0= T2*C0 + T3*C1
    float3 temp57;
    float3 temp58;
    float3 temp59;
    float3 temp60;
    temp60= saturate(tex2.rgb);
    float3 temp61;
    float3 temp62;
    temp62= c2.rgb;
    float3 temp63;
    float3 temp64;
    temp64= temp60 * temp62;
    temp58= temp64;
    float3 temp65;
    temp65= saturate(tex3.rgb);
    float3 temp66;
    float3 temp67;
    temp67= c3.rgb;
    float3 temp68;
    float3 temp69;
    temp69= temp65 * temp67;
    temp59= temp69;
    temp57= temp58 + temp59;
    // exp.components=1 term=1 term=1
    // line 45: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp70;
    float temp71;
    float temp72;
    float temp73;
    temp73= tex2.b;
    float3 temp74;
    float temp75;
    temp75= r0.b;
    float3 temp76;
    float temp77;
    temp77= temp73 * temp75;
    temp71= temp77;
    float temp78;
    temp78= 0.5;
    float3 temp79;
    float temp80;
    temp80= r1.b;
    float3 temp81;
    float temp82;
    temp82= temp78 * temp80;
    temp72= temp82;
    temp70= temp71 - temp72;
    temp70= 2 * temp70;
    r0.rgb= temp57;
    r1.a= temp70;
    // exp.components=3 term=3 term=3
    // line 50: D1= D0a*C0 + INVERT(D0a)*C1
    float3 temp83;
    float3 temp84;
    float3 temp85;
    float temp86;
    temp86= saturate(color0.a);
    float3 temp87;
    float3 temp88;
    temp88= c4.rgb;
    float3 temp89;
    float3 temp90;
    temp90= temp86 * temp88;
    temp84= temp90;
    float temp91;
    temp91= 1-saturate(color0.a);
    float3 temp92;
    float3 temp93;
    temp93= c5.rgb;
    float3 temp94;
    float3 temp95;
    temp95= temp91 * temp93;
    temp85= temp95;
    temp83= temp84 + temp85;
    // exp.components=1 term=1
    // line 51: D0a= R1a*R1a
    float temp96;
    float temp97;
    temp97= saturate(r1.a);
    float3 temp98;
    float temp99;
    temp99= saturate(r1.a);
    float3 temp100;
    float temp101;
    temp101= temp97 * temp99;
    temp96= temp101;
    // exp.components=1 term=1
    // line 52: T1a= EXPAND_NEGATE(T2b)
    float temp102;
    float temp103;
    temp103= 1 - 2 * saturate(tex2.b);
    float3 temp104;
    temp102= temp103;
    color1.rgb= temp83;
    color0.a= temp96;
    tex1.a= temp102;
    // exp.components=3 term=3
    // line 56: D1= OUT_SCALE_BY_2(D1*C0)
    float3 temp105;
    float3 temp106;
    temp106= saturate(color1.rgb);
    float3 temp107;
    float3 temp108;
    temp108= c6.rgb;
    float3 temp109;
    float3 temp110;
    temp110= temp106 * temp108;
    temp105= temp110;
    temp105= 2 * temp105;
    // exp.components=1 term=1
    // line 57: R1a= D0a*D0a
    float temp111;
    float temp112;
    temp112= saturate(color0.a);
    float3 temp113;
    float temp114;
    temp114= saturate(color0.a);
    float3 temp115;
    float temp116;
    temp116= temp112 * temp114;
    temp111= temp116;
    // exp.components=1 term=1
    // line 58: T1a= EXPAND(T1a)
    float temp117;
    float temp118;
    temp118= 2 * saturate(tex1.a) - 1;
    float3 temp119;
    temp117= temp118;
    color1.rgb= temp105;
    r1.a= temp111;
    tex1.a= temp117;
    // exp.components=3 term=3
    // line 64: T2= T2a*C0
    float3 temp120;
    float temp121;
    temp121= saturate(tex2.a);
    float3 temp122;
    float3 temp123;
    temp123= c7.rgb;
    float3 temp124;
    float3 temp125;
    temp125= temp121 * temp123;
    temp120= temp125;
    // exp.components=1 term=1
    // line 66: R1a= 0
    float temp126;
    float temp127;
    temp127= 0;
    float3 temp128;
    temp126= temp127;
    tex2.rgb= temp120;
    r1.a= temp126;
    // exp.components=3 term=3 term=3
    // line 88: R0= OUT_SCALE_BY_2(R0*T0 + T2*T0)
    float3 temp129;
    float3 temp130;
    float3 temp131;
    float3 temp132;
    temp132= saturate(r0.rgb);
    float3 temp133;
    float3 temp134;
    temp134= saturate(tex0.rgb);
    float3 temp135;
    float3 temp136;
    temp136= temp132 * temp134;
    temp130= temp136;
    float3 temp137;
    temp137= saturate(tex2.rgb);
    float3 temp138;
    float3 temp139;
    temp139= saturate(tex0.rgb);
    float3 temp140;
    float3 temp141;
    temp141= temp137 * temp139;
    temp131= temp141;
    temp129= temp130 + temp131;
    temp129= 2 * temp129;
    // exp.components=1 term=1
    // line 89: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp142;
    float temp143;
    temp143= saturate(r1.a);
    float3 temp144;
    float temp145;
    temp145= saturate(tex0.a);
    float3 temp146;
    float temp147;
    temp147= temp143 * temp145;
    temp142= temp147;
    temp142= 2 * temp142;
    // exp.components=1 term=1
    // line 90: T1a= EXPAND(T1a)
    float temp148;
    float temp149;
    temp149= 2 * saturate(tex1.a) - 1;
    float3 temp150;
    temp148= temp149;
    r0.rgb= temp129;
    r1.a= temp142;
    tex1.a= temp148;
    // exp.components=3 term=3
    // line 93: D1= OUT_SCALE_BY_2(D1*R1a)
    float3 temp151;
    float3 temp152;
    temp152= saturate(color1.rgb);
    float3 temp153;
    float temp154;
    temp154= saturate(r1.a);
    float3 temp155;
    float3 temp156;
    temp156= temp152 * temp154;
    temp151= temp156;
    temp151= 2 * temp151;
    // exp.components=1 term=1 term=1
    // line 95: R0a= 0 mux 1
    float temp157;
    float temp158;
    float temp159;
    float temp160;
    temp160= 0;
    float3 temp161;
    temp158= temp160;
    float temp162;
    temp162= 1;
    float3 temp163;
    temp159= temp162;
    temp157= lerp(temp158,temp159,round(r0.a));
    color1.rgb= temp151;
    r0.a= temp157;
    // exp.components=3 term=3 term=3
    // line 97: SRCCOLOR= D1*INVERT(T1a) + R0
    float3 temp164;
    float3 temp165;
    float3 temp166;
    float3 temp167;
    temp167= saturate(color1.rgb);
    float3 temp168;
    float temp169;
    temp169= 1-saturate(tex1.a);
    float3 temp170;
    float3 temp171;
    temp171= temp167 * temp169;
    temp165= temp171;
    float3 temp172;
    temp172= saturate(r0.rgb);
    float3 temp173;
    temp166= temp172;
    temp164= temp165 + temp166;
    // exp.components=1 term=1
    // line 98: SRCALPHA= R0a
    float temp174;
    float temp175;
    temp175= saturate(r0.a);
    float3 temp176;
    temp174= temp175;
    r0.rgb= temp164;
    r0.a= temp174;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=1
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=3 term=3
    // line 24: R0= OUT_SCALE_BY_2(T0*T1)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex1.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 25: R0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    r0.rgb= temp0;
    r0.a= temp6;
    // exp.components=3 term=3
    // line 27: T0= OUT_SCALE_BY_2(R0*T4)
    float3 temp12;
    float3 temp13;
    temp13= saturate(r0.rgb);
    float3 temp14;
    float3 temp15;
    temp15= saturate(tex4.rgb);
    float3 temp16;
    float3 temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    temp12= 2 * temp12;
    // exp.components=1 term=1
    // line 28: R0a= R0a*T1a
    float temp18;
    float temp19;
    temp19= saturate(r0.a);
    float3 temp20;
    float temp21;
    temp21= saturate(tex1.a);
    float3 temp22;
    float temp23;
    temp23= temp19 * temp21;
    temp18= temp23;
    tex0.rgb= temp12;
    r0.a= temp18;
    // exp.components=1 term=1
    // line 31: R0= EXPAND(T2).EXPAND(D0)
    float temp24;
    float3 temp25;
    temp25= 2 * saturate(tex2.rgb) - 1;
    float3 temp26;
    float3 temp27;
    temp27= 2 * saturate(color0.rgb) - 1;
    float3 temp28;
    float temp29;
    temp29= dot(temp25, temp27);
    temp24= temp29;
    // exp.components=1 term=1
    // line 32: R1= EXPAND(T3).EXPAND(D0)
    float temp30;
    float3 temp31;
    temp31= 2 * saturate(tex3.rgb) - 1;
    float3 temp32;
    float3 temp33;
    temp33= 2 * saturate(color0.rgb) - 1;
    float3 temp34;
    float temp35;
    temp35= dot(temp31, temp33);
    temp30= temp35;
    // exp.components=1 term=1
    // line 33: T3a= C0a
    float temp36;
    float temp37;
    temp37= 0.752941;
    float3 temp38;
    temp36= temp37;
    r0.rgb= temp24;
    r1.rgb= temp30;
    tex3.a= temp36;
    // exp.components=1 term=1
    // line 36: T2= EXPAND(T2).EXPAND(T3)
    float temp39;
    float3 temp40;
    temp40= 2 * saturate(tex2.rgb) - 1;
    float3 temp41;
    float3 temp42;
    temp42= 2 * saturate(tex3.rgb) - 1;
    float3 temp43;
    float temp44;
    temp44= dot(temp40, temp42);
    temp39= temp44;
    // exp.components=1 term=1
    // line 37: T3= EXPAND(T2).EXPAND(D1)
    float temp45;
    float3 temp46;
    temp46= 2 * saturate(tex2.rgb) - 1;
    float3 temp47;
    float3 temp48;
    temp48= 2 * saturate(color1.rgb) - 1;
    float3 temp49;
    float temp50;
    temp50= dot(temp46, temp48);
    temp45= temp50;
    // exp.components=1 term=1
    // line 38: D0a= R0b
    float temp51;
    float temp52;
    temp52= saturate(r0.b);
    float3 temp53;
    temp51= temp52;
    // exp.components=1 term=1
    // line 39: T2a= EXPAND(T2b)
    float temp54;
    float temp55;
    temp55= 2 * saturate(tex2.b) - 1;
    float3 temp56;
    temp54= temp55;
    tex2.rgb= temp39;
    tex3.rgb= temp45;
    color0.a= temp51;
    tex2.a= temp54;
    // exp.components=3 term=3 term=3
    // line 44: R0= T2*C0 + T3*C1
    float3 temp57;
    float3 temp58;
    float3 temp59;
    float3 temp60;
    temp60= saturate(tex2.rgb);
    float3 temp61;
    float3 temp62;
    temp62= c2.rgb;
    float3 temp63;
    float3 temp64;
    temp64= temp60 * temp62;
    temp58= temp64;
    float3 temp65;
    temp65= saturate(tex3.rgb);
    float3 temp66;
    float3 temp67;
    temp67= c3.rgb;
    float3 temp68;
    float3 temp69;
    temp69= temp65 * temp67;
    temp59= temp69;
    temp57= temp58 + temp59;
    // exp.components=1 term=1 term=1
    // line 45: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp70;
    float temp71;
    float temp72;
    float temp73;
    temp73= tex2.b;
    float3 temp74;
    float temp75;
    temp75= r0.b;
    float3 temp76;
    float temp77;
    temp77= temp73 * temp75;
    temp71= temp77;
    float temp78;
    temp78= 0.5;
    float3 temp79;
    float temp80;
    temp80= r1.b;
    float3 temp81;
    float temp82;
    temp82= temp78 * temp80;
    temp72= temp82;
    temp70= temp71 - temp72;
    temp70= 2 * temp70;
    r0.rgb= temp57;
    r1.a= temp70;
    // exp.components=3 term=3 term=3
    // line 50: D1= D0a*C0 + INVERT(D0a)*C1
    float3 temp83;
    float3 temp84;
    float3 temp85;
    float temp86;
    temp86= saturate(color0.a);
    float3 temp87;
    float3 temp88;
    temp88= c4.rgb;
    float3 temp89;
    float3 temp90;
    temp90= temp86 * temp88;
    temp84= temp90;
    float temp91;
    temp91= 1-saturate(color0.a);
    float3 temp92;
    float3 temp93;
    temp93= c5.rgb;
    float3 temp94;
    float3 temp95;
    temp95= temp91 * temp93;
    temp85= temp95;
    temp83= temp84 + temp85;
    // exp.components=1 term=1
    // line 51: D0a= R1a*R1a
    float temp96;
    float temp97;
    temp97= saturate(r1.a);
    float3 temp98;
    float temp99;
    temp99= saturate(r1.a);
    float3 temp100;
    float temp101;
    temp101= temp97 * temp99;
    temp96= temp101;
    // exp.components=1 term=1
    // line 52: T1a= EXPAND_NEGATE(T2b)
    float temp102;
    float temp103;
    temp103= 1 - 2 * saturate(tex2.b);
    float3 temp104;
    temp102= temp103;
    color1.rgb= temp83;
    color0.a= temp96;
    tex1.a= temp102;
    // exp.components=3 term=3
    // line 56: D1= OUT_SCALE_BY_2(D1*C0)
    float3 temp105;
    float3 temp106;
    temp106= saturate(color1.rgb);
    float3 temp107;
    float3 temp108;
    temp108= c6.rgb;
    float3 temp109;
    float3 temp110;
    temp110= temp106 * temp108;
    temp105= temp110;
    temp105= 2 * temp105;
    // exp.components=1 term=1
    // line 57: R1a= D0a*D0a
    float temp111;
    float temp112;
    temp112= saturate(color0.a);
    float3 temp113;
    float temp114;
    temp114= saturate(color0.a);
    float3 temp115;
    float temp116;
    temp116= temp112 * temp114;
    temp111= temp116;
    // exp.components=1 term=1
    // line 58: T1a= EXPAND(T1a)
    float temp117;
    float temp118;
    temp118= 2 * saturate(tex1.a) - 1;
    float3 temp119;
    temp117= temp118;
    color1.rgb= temp105;
    r1.a= temp111;
    tex1.a= temp117;
    // exp.components=3 term=3
    // line 70: T2= T2a*C0
    float3 temp120;
    float temp121;
    temp121= saturate(tex2.a);
    float3 temp122;
    float3 temp123;
    temp123= c7.rgb;
    float3 temp124;
    float3 temp125;
    temp125= temp121 * temp123;
    temp120= temp125;
    // exp.components=1 term=1
    // line 72: R1a= R1a*R1a
    float temp126;
    float temp127;
    temp127= saturate(r1.a);
    float3 temp128;
    float temp129;
    temp129= saturate(r1.a);
    float3 temp130;
    float temp131;
    temp131= temp127 * temp129;
    temp126= temp131;
    tex2.rgb= temp120;
    r1.a= temp126;
    // exp.components=3 term=3 term=3
    // line 88: R0= OUT_SCALE_BY_2(R0*T0 + T2*T0)
    float3 temp132;
    float3 temp133;
    float3 temp134;
    float3 temp135;
    temp135= saturate(r0.rgb);
    float3 temp136;
    float3 temp137;
    temp137= saturate(tex0.rgb);
    float3 temp138;
    float3 temp139;
    temp139= temp135 * temp137;
    temp133= temp139;
    float3 temp140;
    temp140= saturate(tex2.rgb);
    float3 temp141;
    float3 temp142;
    temp142= saturate(tex0.rgb);
    float3 temp143;
    float3 temp144;
    temp144= temp140 * temp142;
    temp134= temp144;
    temp132= temp133 + temp134;
    temp132= 2 * temp132;
    // exp.components=1 term=1
    // line 89: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp145;
    float temp146;
    temp146= saturate(r1.a);
    float3 temp147;
    float temp148;
    temp148= saturate(tex0.a);
    float3 temp149;
    float temp150;
    temp150= temp146 * temp148;
    temp145= temp150;
    temp145= 2 * temp145;
    // exp.components=1 term=1
    // line 90: T1a= EXPAND(T1a)
    float temp151;
    float temp152;
    temp152= 2 * saturate(tex1.a) - 1;
    float3 temp153;
    temp151= temp152;
    r0.rgb= temp132;
    r1.a= temp145;
    tex1.a= temp151;
    // exp.components=3 term=3
    // line 93: D1= OUT_SCALE_BY_2(D1*R1a)
    float3 temp154;
    float3 temp155;
    temp155= saturate(color1.rgb);
    float3 temp156;
    float temp157;
    temp157= saturate(r1.a);
    float3 temp158;
    float3 temp159;
    temp159= temp155 * temp157;
    temp154= temp159;
    temp154= 2 * temp154;
    // exp.components=1 term=1 term=1
    // line 95: R0a= 0 mux 1
    float temp160;
    float temp161;
    float temp162;
    float temp163;
    temp163= 0;
    float3 temp164;
    temp161= temp163;
    float temp165;
    temp165= 1;
    float3 temp166;
    temp162= temp165;
    temp160= lerp(temp161,temp162,round(r0.a));
    color1.rgb= temp154;
    r0.a= temp160;
    // exp.components=3 term=3 term=3
    // line 97: SRCCOLOR= D1*INVERT(T1a) + R0
    float3 temp167;
    float3 temp168;
    float3 temp169;
    float3 temp170;
    temp170= saturate(color1.rgb);
    float3 temp171;
    float temp172;
    temp172= 1-saturate(tex1.a);
    float3 temp173;
    float3 temp174;
    temp174= temp170 * temp172;
    temp168= temp174;
    float3 temp175;
    temp175= saturate(r0.rgb);
    float3 temp176;
    temp169= temp175;
    temp167= temp168 + temp169;
    // exp.components=1 term=1
    // line 98: SRCALPHA= R0a
    float temp177;
    float temp178;
    temp178= saturate(r0.a);
    float3 temp179;
    temp177= temp178;
    r0.rgb= temp167;
    r0.a= temp177;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=2
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=3 term=3
    // line 24: R0= OUT_SCALE_BY_2(T0*T1)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex1.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 25: R0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    r0.rgb= temp0;
    r0.a= temp6;
    // exp.components=3 term=3
    // line 27: T0= OUT_SCALE_BY_2(R0*T4)
    float3 temp12;
    float3 temp13;
    temp13= saturate(r0.rgb);
    float3 temp14;
    float3 temp15;
    temp15= saturate(tex4.rgb);
    float3 temp16;
    float3 temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    temp12= 2 * temp12;
    // exp.components=1 term=1
    // line 28: R0a= R0a*T1a
    float temp18;
    float temp19;
    temp19= saturate(r0.a);
    float3 temp20;
    float temp21;
    temp21= saturate(tex1.a);
    float3 temp22;
    float temp23;
    temp23= temp19 * temp21;
    temp18= temp23;
    tex0.rgb= temp12;
    r0.a= temp18;
    // exp.components=1 term=1
    // line 31: R0= EXPAND(T2).EXPAND(D0)
    float temp24;
    float3 temp25;
    temp25= 2 * saturate(tex2.rgb) - 1;
    float3 temp26;
    float3 temp27;
    temp27= 2 * saturate(color0.rgb) - 1;
    float3 temp28;
    float temp29;
    temp29= dot(temp25, temp27);
    temp24= temp29;
    // exp.components=1 term=1
    // line 32: R1= EXPAND(T3).EXPAND(D0)
    float temp30;
    float3 temp31;
    temp31= 2 * saturate(tex3.rgb) - 1;
    float3 temp32;
    float3 temp33;
    temp33= 2 * saturate(color0.rgb) - 1;
    float3 temp34;
    float temp35;
    temp35= dot(temp31, temp33);
    temp30= temp35;
    // exp.components=1 term=1
    // line 33: T3a= C0a
    float temp36;
    float temp37;
    temp37= 0.752941;
    float3 temp38;
    temp36= temp37;
    r0.rgb= temp24;
    r1.rgb= temp30;
    tex3.a= temp36;
    // exp.components=1 term=1
    // line 36: T2= EXPAND(T2).EXPAND(T3)
    float temp39;
    float3 temp40;
    temp40= 2 * saturate(tex2.rgb) - 1;
    float3 temp41;
    float3 temp42;
    temp42= 2 * saturate(tex3.rgb) - 1;
    float3 temp43;
    float temp44;
    temp44= dot(temp40, temp42);
    temp39= temp44;
    // exp.components=1 term=1
    // line 37: T3= EXPAND(T2).EXPAND(D1)
    float temp45;
    float3 temp46;
    temp46= 2 * saturate(tex2.rgb) - 1;
    float3 temp47;
    float3 temp48;
    temp48= 2 * saturate(color1.rgb) - 1;
    float3 temp49;
    float temp50;
    temp50= dot(temp46, temp48);
    temp45= temp50;
    // exp.components=1 term=1
    // line 38: D0a= R0b
    float temp51;
    float temp52;
    temp52= saturate(r0.b);
    float3 temp53;
    temp51= temp52;
    // exp.components=1 term=1
    // line 39: T2a= EXPAND(T2b)
    float temp54;
    float temp55;
    temp55= 2 * saturate(tex2.b) - 1;
    float3 temp56;
    temp54= temp55;
    tex2.rgb= temp39;
    tex3.rgb= temp45;
    color0.a= temp51;
    tex2.a= temp54;
    // exp.components=3 term=3 term=3
    // line 44: R0= T2*C0 + T3*C1
    float3 temp57;
    float3 temp58;
    float3 temp59;
    float3 temp60;
    temp60= saturate(tex2.rgb);
    float3 temp61;
    float3 temp62;
    temp62= c2.rgb;
    float3 temp63;
    float3 temp64;
    temp64= temp60 * temp62;
    temp58= temp64;
    float3 temp65;
    temp65= saturate(tex3.rgb);
    float3 temp66;
    float3 temp67;
    temp67= c3.rgb;
    float3 temp68;
    float3 temp69;
    temp69= temp65 * temp67;
    temp59= temp69;
    temp57= temp58 + temp59;
    // exp.components=1 term=1 term=1
    // line 45: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp70;
    float temp71;
    float temp72;
    float temp73;
    temp73= tex2.b;
    float3 temp74;
    float temp75;
    temp75= r0.b;
    float3 temp76;
    float temp77;
    temp77= temp73 * temp75;
    temp71= temp77;
    float temp78;
    temp78= 0.5;
    float3 temp79;
    float temp80;
    temp80= r1.b;
    float3 temp81;
    float temp82;
    temp82= temp78 * temp80;
    temp72= temp82;
    temp70= temp71 - temp72;
    temp70= 2 * temp70;
    r0.rgb= temp57;
    r1.a= temp70;
    // exp.components=3 term=3 term=3
    // line 50: D1= D0a*C0 + INVERT(D0a)*C1
    float3 temp83;
    float3 temp84;
    float3 temp85;
    float temp86;
    temp86= saturate(color0.a);
    float3 temp87;
    float3 temp88;
    temp88= c4.rgb;
    float3 temp89;
    float3 temp90;
    temp90= temp86 * temp88;
    temp84= temp90;
    float temp91;
    temp91= 1-saturate(color0.a);
    float3 temp92;
    float3 temp93;
    temp93= c5.rgb;
    float3 temp94;
    float3 temp95;
    temp95= temp91 * temp93;
    temp85= temp95;
    temp83= temp84 + temp85;
    // exp.components=1 term=1
    // line 51: D0a= R1a*R1a
    float temp96;
    float temp97;
    temp97= saturate(r1.a);
    float3 temp98;
    float temp99;
    temp99= saturate(r1.a);
    float3 temp100;
    float temp101;
    temp101= temp97 * temp99;
    temp96= temp101;
    // exp.components=1 term=1
    // line 52: T1a= EXPAND_NEGATE(T2b)
    float temp102;
    float temp103;
    temp103= 1 - 2 * saturate(tex2.b);
    float3 temp104;
    temp102= temp103;
    color1.rgb= temp83;
    color0.a= temp96;
    tex1.a= temp102;
    // exp.components=3 term=3
    // line 56: D1= OUT_SCALE_BY_2(D1*C0)
    float3 temp105;
    float3 temp106;
    temp106= saturate(color1.rgb);
    float3 temp107;
    float3 temp108;
    temp108= c6.rgb;
    float3 temp109;
    float3 temp110;
    temp110= temp106 * temp108;
    temp105= temp110;
    temp105= 2 * temp105;
    // exp.components=1 term=1
    // line 57: R1a= D0a*D0a
    float temp111;
    float temp112;
    temp112= saturate(color0.a);
    float3 temp113;
    float temp114;
    temp114= saturate(color0.a);
    float3 temp115;
    float temp116;
    temp116= temp112 * temp114;
    temp111= temp116;
    // exp.components=1 term=1
    // line 58: T1a= EXPAND(T1a)
    float temp117;
    float temp118;
    temp118= 2 * saturate(tex1.a) - 1;
    float3 temp119;
    temp117= temp118;
    color1.rgb= temp105;
    r1.a= temp111;
    tex1.a= temp117;
    // exp.components=3 term=3
    // line 76: T2= T2a*C0
    float3 temp120;
    float temp121;
    temp121= saturate(tex2.a);
    float3 temp122;
    float3 temp123;
    temp123= c7.rgb;
    float3 temp124;
    float3 temp125;
    temp125= temp121 * temp123;
    temp120= temp125;
    // exp.components=1 term=1
    // line 78: R1a= D0a
    float temp126;
    float temp127;
    temp127= saturate(color0.a);
    float3 temp128;
    temp126= temp127;
    tex2.rgb= temp120;
    r1.a= temp126;
    // exp.components=3 term=3 term=3
    // line 88: R0= OUT_SCALE_BY_2(R0*T0 + T2*T0)
    float3 temp129;
    float3 temp130;
    float3 temp131;
    float3 temp132;
    temp132= saturate(r0.rgb);
    float3 temp133;
    float3 temp134;
    temp134= saturate(tex0.rgb);
    float3 temp135;
    float3 temp136;
    temp136= temp132 * temp134;
    temp130= temp136;
    float3 temp137;
    temp137= saturate(tex2.rgb);
    float3 temp138;
    float3 temp139;
    temp139= saturate(tex0.rgb);
    float3 temp140;
    float3 temp141;
    temp141= temp137 * temp139;
    temp131= temp141;
    temp129= temp130 + temp131;
    temp129= 2 * temp129;
    // exp.components=1 term=1
    // line 89: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp142;
    float temp143;
    temp143= saturate(r1.a);
    float3 temp144;
    float temp145;
    temp145= saturate(tex0.a);
    float3 temp146;
    float temp147;
    temp147= temp143 * temp145;
    temp142= temp147;
    temp142= 2 * temp142;
    // exp.components=1 term=1
    // line 90: T1a= EXPAND(T1a)
    float temp148;
    float temp149;
    temp149= 2 * saturate(tex1.a) - 1;
    float3 temp150;
    temp148= temp149;
    r0.rgb= temp129;
    r1.a= temp142;
    tex1.a= temp148;
    // exp.components=3 term=3
    // line 93: D1= OUT_SCALE_BY_2(D1*R1a)
    float3 temp151;
    float3 temp152;
    temp152= saturate(color1.rgb);
    float3 temp153;
    float temp154;
    temp154= saturate(r1.a);
    float3 temp155;
    float3 temp156;
    temp156= temp152 * temp154;
    temp151= temp156;
    temp151= 2 * temp151;
    // exp.components=1 term=1 term=1
    // line 95: R0a= 0 mux 1
    float temp157;
    float temp158;
    float temp159;
    float temp160;
    temp160= 0;
    float3 temp161;
    temp158= temp160;
    float temp162;
    temp162= 1;
    float3 temp163;
    temp159= temp162;
    temp157= lerp(temp158,temp159,round(r0.a));
    color1.rgb= temp151;
    r0.a= temp157;
    // exp.components=3 term=3 term=3
    // line 97: SRCCOLOR= D1*INVERT(T1a) + R0
    float3 temp164;
    float3 temp165;
    float3 temp166;
    float3 temp167;
    temp167= saturate(color1.rgb);
    float3 temp168;
    float temp169;
    temp169= 1-saturate(tex1.a);
    float3 temp170;
    float3 temp171;
    temp171= temp167 * temp169;
    temp165= temp171;
    float3 temp172;
    temp172= saturate(r0.rgb);
    float3 temp173;
    temp166= temp172;
    temp164= temp165 + temp166;
    // exp.components=1 term=1
    // line 98: SRCALPHA= R0a
    float temp174;
    float temp175;
    temp175= saturate(r0.a);
    float3 temp176;
    temp174= temp175;
    r0.rgb= temp164;
    r0.a= temp174;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
#case $specular_type=3
sampler2D s0 : register(S0);
sampler2D s1 : register(S1);
sampler2D s2 : register(S2);
samplerCUBE s3 : register(S3);
sampler2D s4 : register(S4);
float4 bumpenv : register(C0);    // .r=m00 .g=m01 .b=m10 .a=m11
float4 c1 : register(C1);
float4 c2 : register(C2);
float4 c3 : register(C3);
float4 c4 : register(C4);
float4 c5 : register(C5);
float4 c6 : register(C6);
float4 c7 : register(C7);
float4 zwriteout : register(c16);
struct MRT_return
{
    float4 color :COLOR0;
    float4 depth :COLOR1;
};
#ifndef MRT
float4 main(
#else
MRT_return main(
#endif
#ifdef CENTROID0
    float4 tc0 : TEXCOORD0_centroid,
#else
    float4 tc0 : TEXCOORD0,
#endif
#ifdef CENTROID1
    float4 tc1 : TEXCOORD1_centroid,
#else
    float4 tc1 : TEXCOORD1,
#endif
#ifdef CENTROID2
    float4 tc2 : TEXCOORD2_centroid,
#else
    float4 tc2 : TEXCOORD2,
#endif
#ifdef CENTROID3
    float4 tc3 : TEXCOORD3_centroid,
#else
    float4 tc3 : TEXCOORD3,
#endif
#ifdef CENTROID4
    float4 tc4 : TEXCOORD4_centroid,
#else
    float4 tc4 : TEXCOORD4,
#endif
    float4 tc7:TEXCOORD7,
    float4 color0 : COLOR0,
    float4 color1 : COLOR1
)
#ifndef MRT
    : COLOR
#endif
{
    float4 r0, r1;
    float3 e, f;
    r0.r= r0.g= r0.b= 0;
    r0.a= 1;
    float4 tex5;
    float4 tex6;
    float4 tex7;
// texture stage 0 mode 0 dot 0 input-stage 0
    float4 tex0= tex2Dproj(s0, tc0);
// texture stage 1 mode 0 dot 0 input-stage 0
    float4 tex1= tex2Dproj(s1, tc1);
// texture stage 2 mode 0 dot 0 input-stage 0
    float4 tex2= tex2Dproj(s2, tc2);
// texture stage 3 mode 2 dot 0 input-stage 0
    float4 tex3= texCUBE(s3, tc3);
// texture stage 4 mode 0 dot 0 input-stage 0
    float4 tex4= tex2Dproj(s4, tc4);
    // exp.components=3 term=3
    // line 24: R0= OUT_SCALE_BY_2(T0*T1)
    float3 temp0;
    float3 temp1;
    temp1= saturate(tex0.rgb);
    float3 temp2;
    float3 temp3;
    temp3= saturate(tex1.rgb);
    float3 temp4;
    float3 temp5;
    temp5= temp1 * temp3;
    temp0= temp5;
    temp0= 2 * temp0;
    // exp.components=1 term=1
    // line 25: R0a= T0a*T4a
    float temp6;
    float temp7;
    temp7= saturate(tex0.a);
    float3 temp8;
    float temp9;
    temp9= saturate(tex4.a);
    float3 temp10;
    float temp11;
    temp11= temp7 * temp9;
    temp6= temp11;
    r0.rgb= temp0;
    r0.a= temp6;
    // exp.components=3 term=3
    // line 27: T0= OUT_SCALE_BY_2(R0*T4)
    float3 temp12;
    float3 temp13;
    temp13= saturate(r0.rgb);
    float3 temp14;
    float3 temp15;
    temp15= saturate(tex4.rgb);
    float3 temp16;
    float3 temp17;
    temp17= temp13 * temp15;
    temp12= temp17;
    temp12= 2 * temp12;
    // exp.components=1 term=1
    // line 28: R0a= R0a*T1a
    float temp18;
    float temp19;
    temp19= saturate(r0.a);
    float3 temp20;
    float temp21;
    temp21= saturate(tex1.a);
    float3 temp22;
    float temp23;
    temp23= temp19 * temp21;
    temp18= temp23;
    tex0.rgb= temp12;
    r0.a= temp18;
    // exp.components=1 term=1
    // line 31: R0= EXPAND(T2).EXPAND(D0)
    float temp24;
    float3 temp25;
    temp25= 2 * saturate(tex2.rgb) - 1;
    float3 temp26;
    float3 temp27;
    temp27= 2 * saturate(color0.rgb) - 1;
    float3 temp28;
    float temp29;
    temp29= dot(temp25, temp27);
    temp24= temp29;
    // exp.components=1 term=1
    // line 32: R1= EXPAND(T3).EXPAND(D0)
    float temp30;
    float3 temp31;
    temp31= 2 * saturate(tex3.rgb) - 1;
    float3 temp32;
    float3 temp33;
    temp33= 2 * saturate(color0.rgb) - 1;
    float3 temp34;
    float temp35;
    temp35= dot(temp31, temp33);
    temp30= temp35;
    // exp.components=1 term=1
    // line 33: T3a= C0a
    float temp36;
    float temp37;
    temp37= 0.752941;
    float3 temp38;
    temp36= temp37;
    r0.rgb= temp24;
    r1.rgb= temp30;
    tex3.a= temp36;
    // exp.components=1 term=1
    // line 36: T2= EXPAND(T2).EXPAND(T3)
    float temp39;
    float3 temp40;
    temp40= 2 * saturate(tex2.rgb) - 1;
    float3 temp41;
    float3 temp42;
    temp42= 2 * saturate(tex3.rgb) - 1;
    float3 temp43;
    float temp44;
    temp44= dot(temp40, temp42);
    temp39= temp44;
    // exp.components=1 term=1
    // line 37: T3= EXPAND(T2).EXPAND(D1)
    float temp45;
    float3 temp46;
    temp46= 2 * saturate(tex2.rgb) - 1;
    float3 temp47;
    float3 temp48;
    temp48= 2 * saturate(color1.rgb) - 1;
    float3 temp49;
    float temp50;
    temp50= dot(temp46, temp48);
    temp45= temp50;
    // exp.components=1 term=1
    // line 38: D0a= R0b
    float temp51;
    float temp52;
    temp52= saturate(r0.b);
    float3 temp53;
    temp51= temp52;
    // exp.components=1 term=1
    // line 39: T2a= EXPAND(T2b)
    float temp54;
    float temp55;
    temp55= 2 * saturate(tex2.b) - 1;
    float3 temp56;
    temp54= temp55;
    tex2.rgb= temp39;
    tex3.rgb= temp45;
    color0.a= temp51;
    tex2.a= temp54;
    // exp.components=3 term=3 term=3
    // line 44: R0= T2*C0 + T3*C1
    float3 temp57;
    float3 temp58;
    float3 temp59;
    float3 temp60;
    temp60= saturate(tex2.rgb);
    float3 temp61;
    float3 temp62;
    temp62= c2.rgb;
    float3 temp63;
    float3 temp64;
    temp64= temp60 * temp62;
    temp58= temp64;
    float3 temp65;
    temp65= saturate(tex3.rgb);
    float3 temp66;
    float3 temp67;
    temp67= c3.rgb;
    float3 temp68;
    float3 temp69;
    temp69= temp65 * temp67;
    temp59= temp69;
    temp57= temp58 + temp59;
    // exp.components=1 term=1 term=1
    // line 45: R1a= OUT_SCALE_BY_2(SIGNED(T2b)*SIGNED(R0b) - 1/2*SIGNED(R1b))
    float temp70;
    float temp71;
    float temp72;
    float temp73;
    temp73= tex2.b;
    float3 temp74;
    float temp75;
    temp75= r0.b;
    float3 temp76;
    float temp77;
    temp77= temp73 * temp75;
    temp71= temp77;
    float temp78;
    temp78= 0.5;
    float3 temp79;
    float temp80;
    temp80= r1.b;
    float3 temp81;
    float temp82;
    temp82= temp78 * temp80;
    temp72= temp82;
    temp70= temp71 - temp72;
    temp70= 2 * temp70;
    r0.rgb= temp57;
    r1.a= temp70;
    // exp.components=3 term=3 term=3
    // line 50: D1= D0a*C0 + INVERT(D0a)*C1
    float3 temp83;
    float3 temp84;
    float3 temp85;
    float temp86;
    temp86= saturate(color0.a);
    float3 temp87;
    float3 temp88;
    temp88= c4.rgb;
    float3 temp89;
    float3 temp90;
    temp90= temp86 * temp88;
    temp84= temp90;
    float temp91;
    temp91= 1-saturate(color0.a);
    float3 temp92;
    float3 temp93;
    temp93= c5.rgb;
    float3 temp94;
    float3 temp95;
    temp95= temp91 * temp93;
    temp85= temp95;
    temp83= temp84 + temp85;
    // exp.components=1 term=1
    // line 51: D0a= R1a*R1a
    float temp96;
    float temp97;
    temp97= saturate(r1.a);
    float3 temp98;
    float temp99;
    temp99= saturate(r1.a);
    float3 temp100;
    float temp101;
    temp101= temp97 * temp99;
    temp96= temp101;
    // exp.components=1 term=1
    // line 52: T1a= EXPAND_NEGATE(T2b)
    float temp102;
    float temp103;
    temp103= 1 - 2 * saturate(tex2.b);
    float3 temp104;
    temp102= temp103;
    color1.rgb= temp83;
    color0.a= temp96;
    tex1.a= temp102;
    // exp.components=3 term=3
    // line 56: D1= OUT_SCALE_BY_2(D1*C0)
    float3 temp105;
    float3 temp106;
    temp106= saturate(color1.rgb);
    float3 temp107;
    float3 temp108;
    temp108= c6.rgb;
    float3 temp109;
    float3 temp110;
    temp110= temp106 * temp108;
    temp105= temp110;
    temp105= 2 * temp105;
    // exp.components=1 term=1
    // line 57: R1a= D0a*D0a
    float temp111;
    float temp112;
    temp112= saturate(color0.a);
    float3 temp113;
    float temp114;
    temp114= saturate(color0.a);
    float3 temp115;
    float temp116;
    temp116= temp112 * temp114;
    temp111= temp116;
    // exp.components=1 term=1
    // line 58: T1a= EXPAND(T1a)
    float temp117;
    float temp118;
    temp118= 2 * saturate(tex1.a) - 1;
    float3 temp119;
    temp117= temp118;
    color1.rgb= temp105;
    r1.a= temp111;
    tex1.a= temp117;
    // exp.components=3 term=3
    // line 82: T2= T2a*C0
    float3 temp120;
    float temp121;
    temp121= saturate(tex2.a);
    float3 temp122;
    float3 temp123;
    temp123= c7.rgb;
    float3 temp124;
    float3 temp125;
    temp125= temp121 * temp123;
    temp120= temp125;
    // exp.components=1 term=1 term=1
    // line 84: R1a= OUT_SCALE_BY_4(R1a*R1a - T3a)
    float temp126;
    float temp127;
    float temp128;
    float temp129;
    temp129= saturate(r1.a);
    float3 temp130;
    float temp131;
    temp131= saturate(r1.a);
    float3 temp132;
    float temp133;
    temp133= temp129 * temp131;
    temp127= temp133;
    float temp134;
    temp134= saturate(tex3.a);
    float3 temp135;
    temp128= temp134;
    temp126= temp127 - temp128;
    temp126= 4 * temp126;
    tex2.rgb= temp120;
    r1.a= temp126;
    // exp.components=3 term=3 term=3
    // line 88: R0= OUT_SCALE_BY_2(R0*T0 + T2*T0)
    float3 temp136;
    float3 temp137;
    float3 temp138;
    float3 temp139;
    temp139= saturate(r0.rgb);
    float3 temp140;
    float3 temp141;
    temp141= saturate(tex0.rgb);
    float3 temp142;
    float3 temp143;
    temp143= temp139 * temp141;
    temp137= temp143;
    float3 temp144;
    temp144= saturate(tex2.rgb);
    float3 temp145;
    float3 temp146;
    temp146= saturate(tex0.rgb);
    float3 temp147;
    float3 temp148;
    temp148= temp144 * temp146;
    temp138= temp148;
    temp136= temp137 + temp138;
    temp136= 2 * temp136;
    // exp.components=1 term=1
    // line 89: R1a= OUT_SCALE_BY_2(R1a*T0a)
    float temp149;
    float temp150;
    temp150= saturate(r1.a);
    float3 temp151;
    float temp152;
    temp152= saturate(tex0.a);
    float3 temp153;
    float temp154;
    temp154= temp150 * temp152;
    temp149= temp154;
    temp149= 2 * temp149;
    // exp.components=1 term=1
    // line 90: T1a= EXPAND(T1a)
    float temp155;
    float temp156;
    temp156= 2 * saturate(tex1.a) - 1;
    float3 temp157;
    temp155= temp156;
    r0.rgb= temp136;
    r1.a= temp149;
    tex1.a= temp155;
    // exp.components=3 term=3
    // line 93: D1= OUT_SCALE_BY_2(D1*R1a)
    float3 temp158;
    float3 temp159;
    temp159= saturate(color1.rgb);
    float3 temp160;
    float temp161;
    temp161= saturate(r1.a);
    float3 temp162;
    float3 temp163;
    temp163= temp159 * temp161;
    temp158= temp163;
    temp158= 2 * temp158;
    // exp.components=1 term=1 term=1
    // line 95: R0a= 0 mux 1
    float temp164;
    float temp165;
    float temp166;
    float temp167;
    temp167= 0;
    float3 temp168;
    temp165= temp167;
    float temp169;
    temp169= 1;
    float3 temp170;
    temp166= temp169;
    temp164= lerp(temp165,temp166,round(r0.a));
    color1.rgb= temp158;
    r0.a= temp164;
    // exp.components=3 term=3 term=3
    // line 97: SRCCOLOR= D1*INVERT(T1a) + R0
    float3 temp171;
    float3 temp172;
    float3 temp173;
    float3 temp174;
    temp174= saturate(color1.rgb);
    float3 temp175;
    float temp176;
    temp176= 1-saturate(tex1.a);
    float3 temp177;
    float3 temp178;
    temp178= temp174 * temp176;
    temp172= temp178;
    float3 temp179;
    temp179= saturate(r0.rgb);
    float3 temp180;
    temp173= temp179;
    temp171= temp172 + temp173;
    // exp.components=1 term=1
    // line 98: SRCALPHA= R0a
    float temp181;
    float temp182;
    temp182= saturate(r0.a);
    float3 temp183;
    temp181= temp182;
    r0.rgb= temp171;
    r0.a= temp181;

//fog zvalue
    float linear_depth = tc7.w / zwriteout.z;
    float3 depth_stripe;

    depth_stripe.r = floor(linear_depth*256)/256;
    depth_stripe.g = frac(floor(linear_depth*65536)/256);
    depth_stripe.b = frac(floor(linear_depth*65536*256)/256);

#ifdef ZPRIME
    //r0.a has alpha kill value
    r0.rgb = depth_stripe.rgb;
#endif

#ifndef MRT
    return r0;
#else
    MRT_return retval;
    retval.color = r0;
    retval.depth.rgb = depth_stripe.rgb;
    retval.depth.a = r0.a;
    return retval;
#endif
}
